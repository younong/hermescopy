# 多用户隔离开发计划（强约束、压缩版）

## Context

本计划把 authenticated 多用户隔离按可独立实现和单元测试的迭代交付。目标不是让每条路径“传递 `owner_key`”，而是让可信身份、数据、live state、PTY、prompt、workspace、进程、凭据与执行沙箱具有不可跨 owner 的边界。任一 owner、generation、lease、epoch 或 runtime identity 缺失、陈旧或不一致时必须 fail closed，绝不回退到 global、legacy 或其他 owner runtime。

## 执行契约（最高优先级）

1. **唯一 live 状态：** 本文“执行索引”是唯一执行状态来源。恢复时只读取执行索引、当前迭代和首个未勾选步骤；不得用会话摘要替代本文。
2. **固定分支：** 全部工作只在 `feature/multi-user-owner-workers` 完成；不得为本计划新建、切换或重置分支。
3. **严格顺序：** 只能执行最早未完成迭代的第一个未勾选步骤。前置步骤的代码、单元测试与证据写回前，禁止实施或为实施目的测试后续步骤/迭代。
4. **严格范围：** 仅允许代码和单元测试；禁止应用运行、集成/E2E、smoke、部署、发布、提交、PR，以及 `/run`、`/verify`、各类 review。
5. **可信身份：** owner 仅从可信 HTTP session、已验证 WS ticket 或 server-spawn context 派生。`owner_key`、`profile`、`browser_id`、session ID、cwd、路径和 query 永不作为授权输入。
6. **隔离与拒绝：** authenticated 请求不得访问 Control Plane/global/legacy owner-sensitive state；未迁移 surface 必须 403/404。local/unauthenticated 模式保持显式独立。
7. **测试节奏：** 每步先补 A/B 隔离与失败语义单测，再迁移入口；全部实现完成后先跑最窄 focused 单测，稳定后仅一次相关联合单元回归。失败只定位首个根因的最小复现，并记录命令、结果和根因。
8. **边界职责：** Control Plane 仅认证、授权、路由和不解析内容的 bridge；owner-sensitive DB、live state、路径、进程、凭据和执行器状态必须在 owner scope。
9. **去敏审计：** 仅记录去敏 scope/generation/event/correlation ID；禁止 prompt、会话正文、原始 token/ticket 和敏感文件内容。
10. **Agent 上限：** 本计划生命周期新增子 Agent 总数不得超过 10。唯一下一步骤默认不创建 Agent；只有直接调用链存在已记录且无法消解的矛盾时才可精确调查。
11. **最小阅读集：** 开始每个步骤时，只能先读取执行索引列出的直接入口、直接测试，以及为连接这些入口所必需的**一跳** caller/callee。不得按目录、关键词或文件长度泛读；未获下述例外记录前，禁止阅读后续迭代、未迁移 surface 或无直接调用关系的 PTY/browser/Gateway/recovery 代码。
12. **调用链顺序：** 阅读必须从外部入口沿真实调用链前进：admission/route → trusted context 与 capability/lease → owner-side admission/handler → close/error path → 直接单测。禁止按文件顺序通读、先阅读实现细节后再寻找入口，或在未定位调用链前进行方案设计。
13. **已关闭契约复用：** 已关闭迭代的安全结论是本步骤的前置条件，只验证它们是否在当前调用链被完整接入；不得重新审计、重设计或扩展 OwnerContext、authority/replay、epoch、generation、lease、capability、bootstrap 或 owner-runtime path 的底层实现。发现当前链路未接入时，只记录该接入缺口。
14. **测试反推：** 计划必须先把当前 checkbox 明示的正反例整理为最小单元测试矩阵，再将每一行映射到一个生产改动点。未有 A/B 隔离或失败语义测试行的生产改动不得纳入计划；测试矩阵以外的 feature、重构或“顺便清理”一律禁止。
15. **压缩计划格式：** 计划只能使用“契约 / 当前证据 / 缺口 / 最小改动点 / 单测”五列表；不得写叙述性重述、架构备选或重复前序迭代的设计。计划必须精确指向函数和测试名称，且只覆盖当前唯一步骤。
16. **时间盒与例外：** 定向阅读上限 7 分钟：2 分钟定位最小阅读集、3 分钟走完调用链、2 分钟填充证据；计划上限 4 分钟。到时必须开始先写最小失败语义单测，不得以“再看看”为由扩大范围。只有某项五列表无法填写，且能记录具体缺失符号、调用边或相互矛盾的既有契约时，才可暂停并进行一次精确调查；例外必须写入执行索引的 blocker，调查仍只限消解该矛盾。不得通过运行应用、集成/E2E/smoke、部署或新增 Agent 来规避时间盒。
17. **文档状态原子迁移：** 执行索引、状态总览、当前迭代、checkbox、证据索引和归档结构共同构成一个不可分割的状态机；不得只改其中任一处。只有当前 checkbox 的代码、规定 A/B 负例、focused 与相关联合单元测试及证据全部完成后，才允许更新状态；未完成步骤的进度、checkbox、当前迭代和“下一动作”必须保持不变。
18. **归档固定事务：** 关闭迭代只能在**一次完整编辑**中依次完成：① 写入 append-only 证据（修改文件、精确命令/结果、warnings、首个根因及最小复现）；② 更新执行索引、状态总览和完成进度；③ 勾选已证实 checkbox；④ 以完整 `<details>…</details>` 包裹已关闭迭代；⑤ 在其后创建唯一的 `## 当前迭代` 与首个未勾选步骤；⑥ 保留后续迭代为未开始。禁止先改标题、先折叠区块或先移动当前迭代，再补其余状态。
19. **文档变更后结构校验：** 每次修改执行状态或归档后，必须立刻核对并记录：文件仅有一个 `## 当前迭代`；执行索引、状态总览、当前步骤与 checkbox 进度一致；每个已关闭迭代拥有成对且完整的 `<details>…</details>`；当前迭代不位于归档块内；后续迭代未被误提升为当前；证据索引只追加、不重写历史。任何一项不满足时，停止代码实施，先在同一工作回合恢复文档一致性。
20. **状态与实现分离：** 实施过程不得为了“同步进度”提前修改迭代状态或归档。代码、测试与文档证据先完成；状态迁移仅作为步骤完成后的最后动作。若用户只要求整理文档，不得顺带改变未完成工作、当前步骤或实施范围。

## 执行索引（唯一 live 状态来源）

- **最后更新：** 2026-07-14（Iteration 10 已关闭；I10/3 证据与直接回归已补齐）。
- **当前分支：** `feature/multi-user-owner-workers`；不得新建、切换或重置分支。
- **当前迭代 / 唯一可执行步骤：** 无；用户指定的 Iteration 10 已完成。
- **下一动作：** 停止；未经新的明确计划，不执行后续迭代。
- **禁止越级：** 不实施未计划的后续迭代。
- **当前 blocker：** 无。
- **直接入口 / 测试：** Iteration 10 已归档；I10/5 migration/compatibility regression 已完成。
- **前置安全契约：** I1–I10 已完成可信 OwnerContext、shared authority/replay、exact worker generation/lease capability、pre-import owner runtime isolation 和 session API owner-local routing。authenticated Control Plane 不得读取 owner-sensitive DB 或回退 global/legacy home。

### 状态总览

| 迭代 | 状态 | 进度 | 当前证据 / 唯一入口 |
| --- | --- | --- | --- |
| 1：OwnerContext 和 owner home | 已完成（2026-07-10） | 6/6 | 归档；auth/WS/web-server 回归 454 passed |
| 2：共享授权状态、epoch 和 WS ticket 撤销 | 已完成（2026-07-10） | 6/6 | 归档；相关回归 528 passed、10 warnings |
| 3：Worker generation 和 owner-bound 控制握手 | 已完成（2026-07-11） | 6/6 | 归档；相关回归 483 passed、10 warnings |
| 4：将 session API 迁入 Owner Worker | 已完成（2026-07-11） | 5/5 | 归档；相关回归 490 passed、9 warnings |
| 5：WS、PTY 和浏览器连接边界 | 已完成（2026-07-11） | 4/4 | I5 related Python regression 519 passed、10 warnings；frontend focused 5 passed、typecheck passed |
| 6：Gateway、TUI、resume 和运行时索引 | 已完成（2026-07-11） | 4/4 | 归档；owner-first durable resume/lineage/peer recovery 相关回归 498 passed |
| 7：Safe Filesystem Abstraction | 已完成（2026-07-12） | 5/5 | 归档；I7 path-cache 相关单元回归 258 passed |
| 8：隔离 Tool Executor、凭据和 FD | 已完成（2026-07-12） | 5/5 | 归档；executor-bound process lifecycle 相关单元回归 525 passed、1 warning |
| 9：per-owner execution sandbox 策略 | 已完成（2026-07-13） | 5/5 | 归档；I9 egress policy 相关单元回归 184 passed |
| 10：资源治理、审计和单元回归 | 已完成（2026-07-13） | 5/5 | 归档；I10/5 migration/compatibility 相关单元回归 540 passed、10 existing warnings |

### 完成步骤证据索引（append-only）

此索引是完成步骤的唯一简明证据入口。每项的完整实现叙述、精确命令、结果、warning 与根因保留在同文件的折叠归档或当前迭代记录中；live 索引和状态表不得重复抄录。

| 日期 | 步骤 | 简明安全结果 | 单元测试证据 / 详情 |
| --- | --- | --- | --- |
| 2026-07-10 | I1 / 1–6 | canonical OwnerContext、keyring/rotation、统一可信派生、host/runtime path 与 authenticated selector 拒绝 | 归档 I1：逐步精确命令；最终 `test_dashboard_auth_ws_auth.py` + web-server 相关回归 454 passed（71.59s） |
| 2026-07-10 | I2 / 1–6 | shared authority/replay、ticket epoch/revoke、跨副本 bridge close、rotation/outage fail-closed | 归档 I2：逐步精确命令；最终相关回归 528 passed、10 warnings（93.99s） |
| 2026-07-11 | I3 / 1 | durable monotonic generation 与 exact generation socket | `pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_owner_worker.py`：39 passed（9.32s）；相关回归 467 passed、10 warnings（82.21s） |
| 2026-07-11 | I3 / 2 | durable owner lease/CAS 与 stale cleanup fence | 同 focused：46 passed（9.54s）；相关回归 474 passed、10 warnings（78.11s） |
| 2026-07-11 | I3 / 3 | signed `owc1` exact lease capability，worker 无 signing secret | `pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py::TestAuthenticatedOwnerWorkerSessionProxy`：115 passed（16.82s）；相关回归 474 passed、10 warnings（106.03s） |
| 2026-07-11 | I3 / 4 | one-use bootstrap、`owp1` peer/nonce/sequence fence | authority 6 passed（0.70s）；worker/WS 99 passed（11.31s）；相关回归 478 passed、10 warnings（84.27s） |
| 2026-07-11 | I3 / 5 | pre-import minimal env 与 exact owner runtime paths | `test_owner_runtime.py`：15 passed（0.76s）；admission focused：6 passed（2.39s）；in-process 45 passed（10.23s）；health 1 passed（1.93s）。首因：health fixture 缺 lease/public-verifier env；以最小 fixture 修复，不放宽生产 gate。 |
| 2026-07-11 | I3 / 6 | exact lease 先进入 `DRAINING` 令 capability/bootstrap 失效；关闭 exact-generation bridges 后才终止进程并清理该 generation socket；仅已确认退出才标记 `TERMINATED` | focused authority：`pytest -q tests/hermes_cli/test_dashboard_auth_authority.py -k 'worker_lifecycle_changes or worker_lease_claim_is_fenced'`，2 passed（0.55s）；focused supervisor：`tests/hermes_cli/test_owner_worker.py -k 'fences_and_closes_bridges or active_use_skips_idle_stop'`，2 passed（0.81s）；focused bridge：`tests/hermes_cli/test_dashboard_auth_ws_auth.py -k 'worker_change_closes_only_exact_generation_bridge or authority_change_closes_only_stale_matching_bridge or revoked_authority_change_closes_all_matching_bridge_epochs'`，3 passed（1.80s）；最终 `pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py`，483 passed、10 existing warnings（89.01s）。首因：shared dispatcher fake 缺 `worker_changes_since()`；以完整 fake contract 修复，未降级生产 dispatch。 |
| 2026-07-11 | I4 / 1 | list、search、detail、messages、stats、export、latest-descendant 只读 surface 在 authenticated 模式均经 exact lease capability 代理至 owner-local Worker；Control Plane 的 `SessionDB` 与本地 lineage helper 被断言不可访问，profile aggregation 保持 403。 | focused Control Plane：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_web_server.py::TestAuthenticatedOwnerWorkerSessionProxy -k 'session_read_surfaces or profiles_sessions_fails_closed or templated_session_routes_reject'`，11 passed（2.57s）；focused Worker：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py -k 'session_read_routes_use_owner_db_not_global_sentinel'`，7 passed（2.94s）；最终 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py tests/hermes_cli/test_web_server_session_search.py`，442 passed、10 existing warnings（83.26s）。首因：legacy session-search direct-call test 未传入新增 `request` 参数；以最小 fake request 修复，未放宽 authenticated 路由。 |
| 2026-07-11 | I4 / 2 | rename/patch、single/bulk delete、empty cleanup 和 prune 在 authenticated 模式均代理至 owner-local Worker；Worker 的 request body models 移至模块级，避免 postponed annotation 令 FastAPI 将 body 误判为 query；global sentinel DB 不受写操作影响。resume 解析仍由步骤 1 的 Worker-local messages/lineage helpers 执行，本步骤未引入额外 Control Plane 写路径。 | focused：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_web_server.py::TestAuthenticatedOwnerWorkerSessionProxy -k 'session_write_surfaces' tests/hermes_cli/test_owner_worker.py -k 'session_write_routes_mutate_only_owner_db'`，5 passed（3.10s）；最终 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py tests/gateway/test_session_api.py`，463 passed、9 existing warnings（93.36s）。首因：Worker nested Pydantic body models 配合 postponed annotations 被 FastAPI 视为缺失 query `body`；提升为模块级模型，未削弱 body/profile 校验。 |
| 2026-07-11 | I4 / 3 | `_proxy_authenticated_owner_http()` 从 verified session 重建 OwnerContext，并在 acquire/request 前拒绝 owner-mismatched handle；正常路径使用 exact active handle 的 owner/generation/worker ID/lease/recovery fence 构造 capability，并在响应后释放 use lease。 | focused：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_web_server.py::TestAuthenticatedOwnerWorkerSessionProxy tests/hermes_cli/test_owner_worker.py -k 'capability_is_exact_owner_generation_lease_scope_and_path_bound or capability_rejects_legacy_ow2_and_stale_replacement or supervisor_rejects_same_key'`，3 passed（1.53s）；最终 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_web_server.py`，437 passed、10 existing warnings（80.36s）。首因：新 exact-lease 测试错误假定 Session 自带 owner key；用现有 `owner_context_from_session()` 的可信派生修复。 |
| 2026-07-11 | I4 / 4 | `_open_session_db_for_profile()` 在 authenticated Control Plane 一律 fail closed，阻断未来 route 误入 global/default SessionDB；profile aggregation 维持 403，enumerated owner-worker allowlist 与未迁移 API 403 覆盖继续生效。 | focused：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_web_server.py::TestAuthenticatedOwnerWorkerSessionProxy -k 'profiles_sessions_fails_closed or session_db_helper_fails_closed or unmigrated_apis_fail_closed' tests/hermes_cli/test_dashboard_auth_middleware.py -k 'availability_does_not_prefix_allow_new_session_routes'`，1 passed（2.37s）；最终 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_web_server.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_owner_worker.py`，454 passed、10 existing warnings（105.35s）。无失败根因。 |
| 2026-07-11 | I4 / 5 | detail、messages、export、latest-descendant 的 exact/short ID 与 resume lineage resolve 均在 Worker 的 owner-local `SessionDB` 中完成；B 的 short ID、title 搜索和 resume lineage 对 A 均按不存在（404）或空结果处理。 | focused：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py -k 'session_identifiers_are_resolved_only_within_owner_scope'`，1 passed（1.74s）；最终 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py tests/hermes_cli/test_web_server_session_search.py tests/gateway/test_session_api.py`，490 passed、9 existing warnings（90.89s）。无失败根因。 |
| 2026-07-11 | I5 / 1 | Control Plane bridge 使用 bounded per-direction relay queue、operation deadlines、idempotent close-capable bridge 与 exact-use lease release；session expiry、browser close、connect failure、authority/worker bridge closure 均不读取 PTY/gateway 内容且关闭双方。 | focused：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker_ws_bridge.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_owner_worker.py -k 'bridge or bootstrap or owp1 or websocket or authority_change or worker_change'`，12 passed、107 deselected（2.06s）；相关联合回归：`TMPDIR=/Users/weixiangbin/work/hermescopy/.tmp ~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker_ws_bridge.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_web_server.py`，491 passed、10 existing warnings（102.90s）。首因：`_ws_bridge_close_code()` 未保留 RFC 1013，令 backpressure 被改写为 1011；最小修复后通过。 |
| 2026-07-11 | I5 / 2 | Worker app 显式持有 PTY/browser/event/reconnect breadcrumbs；每个 PTY registration 记录 immutable bootstrap 的 owner、generation、worker ID、lease/recovery、audience/scope/path fence，stale cleanup 不可删除 replacement。authenticated `/api/pty` 已在 Control Plane 仅 bridge，未读其 local PTY maps。 | focused：`TMPDIR=/Users/weixiangbin/work/hermescopy/.tmp ~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_web_server_pty_reconnect.py tests/hermes_cli/test_dashboard_auth_ws_auth.py -k 'pty or browser or reconnect or active_session or bootstrap or generation or lease'`，23 passed、101 deselected（2.52s）；相关联合回归：同三文件，124 passed（20.26s）。首因：legacy `_resolve_chat_argv` test doubles 不接受 app/browser kwargs；仅在 worker app context 传递其新增参数，保留 local legacy resolver contract。 |
| 2026-07-11 | I5 / 3 | frontend owner-key transition coordinator 在同 owner refresh 无操作、A→B 与 logout 前逐项执行 idempotent teardown；ChatPage、sidebar 和 GUI chat 关闭/重置旧 socket、timer、buffer 与 owner UI state，browser bucket 仍仅是本地 namespacing。 | focused：`TMPDIR=/Users/weixiangbin/work/hermescopy/.tmp npm --prefix /Users/weixiangbin/work/hermescopy/web test -- src/lib/browserIdentity.test.ts src/lib/dashboardAuthTransition.test.ts`，2 files / 5 passed（320ms）；`npm --prefix /Users/weixiangbin/work/hermescopy/web run typecheck` passed。无失败根因。 |
| 2026-07-11 | I5 / 4 | authority revoke 继续以 4401 关闭匹配 scope/epoch；worker draining/crash/generation revoke 改为 1011，并由 close-capable bridge 取消 relay、关闭 browser 与 worker halves、exact lease 单次 release。exact worker identity 保证 replacement/different lease 不受影响。 | focused：`TMPDIR=/Users/weixiangbin/work/hermescopy/.tmp ~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_owner_worker.py -k 'bridge or revoke or authority_change or worker_change or generation or lifecycle or close'`，18 passed、101 deselected（2.89s）；最终 I5 related Python regression：`TMPDIR=/Users/weixiangbin/work/hermescopy/.tmp ~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py tests/hermes_cli/test_web_server_pty_reconnect.py`，519 passed、10 existing warnings（94.40s）。首因：local resolver tests 需要保留 `browser_id`/`app_obj` 参数 contract；恢复其 wrapper forwarding，并令旧 reconnect doubles 接受新参数。 |
| 2026-07-11 | I6 / 1 | owner Worker app 绑定 exact owner/generation/worker/lease/recovery Gateway runtime；Control Plane bridge 保持 OWP1，PTY child 只用 worker-local single-use attach URL，过期、重放或跨 app token 均关闭；owner-required TUI 缺 URL 时 fail closed，绝不 spawn `tui_gateway.entry`。 | 修改：`entrypoint.py`、`ws_routes.py`、`tui_gateway/{server,ws}.py`、`ui-tui/src/gatewayClient.ts` 与对应单测。focused：`source .venv/bin/activate && pytest -q tests/hermes_cli/test_owner_worker.py -k 'gateway_runtime_and_attach_tokens or gateway_ws_admits_one_owner_local_tui_attach or chat_argv_requires_owner_worker_gateway_attach'`，3 passed；`npm test --prefix ui-tui -- --run src/__tests__/gatewayClient.test.ts`，14 passed；`pytest -q tests/test_tui_gateway_ws.py -k 'owner_worker_ws_rejects_missing_runtime'`，1 passed。相关回归：`pytest -q tests/hermes_cli/test_owner_worker.py tests/test_tui_gateway_ws.py`，55 passed（16.40s）；`npm run typecheck --prefix ui-tui` passed；`git diff --check` passed。首因：首次从仓库根运行 `npm test` 缺少 script；最小修复为使用 `--prefix ui-tui`，未改生产行为。 |
| 2026-07-11 | I6 / 2 | exact owner runtime 现持有独立 live session、pending prompt/answer、child mirror 与 active-child-run maps 及关联 locks；同 key 的 A/B runtime 不共享状态。deferred build、WS orphan/cap timer、late MCP refresh、notification poller、prompt/background/preview threads 均显式重绑 runtime；standalone reaper 固定只操作独立 local state。session/channel/alias/mirror persisted index 继续按动态 owner home 和现有 owner filter 解析。 | 修改：`tui_gateway/server.py`、`tests/test_tui_gateway_server.py`、`tests/gateway/test_session_store_owner_filter.py`。focused：`source .venv/bin/activate && pytest -q tests/test_tui_gateway_server.py -k 'owner_gateway_runtimes_keep_live_maps_and_prompt_state_isolated or dispatch_binds_owner_runtime_for_registered_handler or owner_runtime_propagates_to_deferred_agent_build or owner_runtime_propagates_to_notification_poller or owner_runtime_propagates_to_late_mcp_refresh or owner_runtime_propagates_to_owner_scoped_timer_callbacks'`，6 passed、307 deselected；`tests/gateway/test_session_store_owner_filter.py`，3 passed；`tests/gateway/test_channel_directory.py tests/gateway/test_mirror.py`，60 passed。相关回归：`pytest -q tests/test_tui_gateway_server.py tests/test_tui_gateway_ws.py tests/gateway/test_session_store_owner_filter.py tests/gateway/test_channel_directory.py tests/gateway/test_mirror.py`，382 passed（55.65s）；`python3 -m compileall -q tui_gateway/server.py` 与 `git diff --check` passed。首因：browser launch-hint 负例在 Darwin 的 manual Chrome fallback 下未进入“no executable”分支；仅在该单测固定 `manual_chrome_debug_command()` 返回 `None`，未改变生产行为。另：session-store test mock 原指向不存在的 module-global `gateway.session.SessionDB`；改为实际 local import `hermes_state.SessionDB`。 |
| 2026-07-11 | I6 / 3 | authenticated worker 写入的 session index、session origin、channel directory 均携带 trusted owner、canonical workspace 与 worker generation；session/channel/alias/mirror 读取在任何缺失、无效、陈旧或不匹配断言时按不存在处理，并仅写入固定的 Control Plane 去敏审计事件。 | 修改：`gateway/session.py`、`gateway/channel_directory.py`、`hermes_cli/dashboard_auth/audit.py` 及 session/channel/mirror/audit 单测。focused：`source .venv/bin/activate && pytest -q tests/gateway/test_session_store_owner_filter.py -k 'missing_or_invalid_scope or matching_workspace_generation or workspace_or_generation_mismatch or writes_current_workspace'`，6 passed、3 deselected（0.52s）；`source .venv/bin/activate && pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/gateway/test_session_store_owner_filter.py tests/gateway/test_channel_directory.py tests/gateway/test_mirror.py tests/test_tui_gateway_owner_workspace_cwd.py`，90 passed（2.75s）。相关联合回归：`source .venv/bin/activate && pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_audit.py tests/gateway/test_session_store_owner_filter.py tests/gateway/test_channel_directory.py tests/gateway/test_mirror.py tests/test_tui_gateway_owner_workspace_cwd.py`，139 passed（17.18s）；`git diff --check` passed。无 warnings；无失败根因。 |
| 2026-07-11 | I6 / 4 | owner-worker resume 先以最小 durable owner/workspace/generation record resolve 并验证，才允许 descendant、child/liveness、live state、reopen、history 或 agent 读取；SQLite compression lineage、ancestor history 与 stale peer recovery 保持 exact durable scope，跨 owner/missing/stale candidate 统一按不存在处理。 | 修改：`hermes_state.py`、`gateway/session.py`、`tui_gateway/server.py` 及 owner-resume、lineage、runtime stale-recovery 单测。focused：`source .venv/bin/activate && pytest -q tests/hermes_state/test_resolve_resume_session_id.py tests/test_tui_gateway_owner_workspace_cwd.py tests/gateway/test_session_store_runtime_stale_guard.py`，35 passed（3.88s）；相关联合回归：`source .venv/bin/activate && pytest -q tests/test_tui_gateway_server.py tests/test_tui_gateway_owner_workspace_cwd.py tests/gateway/test_session_store_owner_filter.py tests/gateway/test_session_store_stale_prune.py tests/gateway/test_session_store_runtime_stale_guard.py tests/gateway/test_mirror.py tests/hermes_state/test_resolve_resume_session_id.py`，389 passed（68.74s）；完整 I6 回归：`source .venv/bin/activate && pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_audit.py tests/test_tui_gateway_server.py tests/test_tui_gateway_ws.py tests/gateway/test_session_store_owner_filter.py tests/gateway/test_session_store_stale_prune.py tests/gateway/test_session_store_runtime_stale_guard.py tests/gateway/test_channel_directory.py tests/gateway/test_mirror.py tests/test_tui_gateway_owner_workspace_cwd.py tests/hermes_state/test_resolve_resume_session_id.py && git diff --check`，498 passed（136.13s）。无 warnings。首因：完整回归中的两个 owner CWD compatibility tests 继承 strict generation 后走入正确的 fail-closed recovery gate；测试 helper 清除 generation，使其显式覆盖 owner-only compatibility，未放宽生产 gate。 |
| 2026-07-12 | I7 / 1 | authenticated owner Worker 在 validated owner runtime 后持有四个不可继承目录 FD：模块位置派生的 policy-readonly global root、owner home、canonical workspace 和 owner-private `runtime/tmp`；构造失败关闭已取得 FD，app lifespan shutdown 确定性关闭；本步未迁移任何 pathname 操作，也不声称已具备 traversal/TOCTOU 防护。 | 修改：`hermes_cli/controlled_roots.py`、`hermes_cli/owner_runtime.py`、`hermes_cli/owner_worker/entrypoint.py`、`tests/hermes_cli/test_controlled_roots.py`、`tests/hermes_cli/test_owner_runtime.py`、`tests/hermes_cli/test_owner_worker.py`。focused：`.venv/bin/python -m pytest tests/hermes_cli/test_controlled_roots.py tests/hermes_cli/test_owner_runtime.py tests/hermes_cli/test_owner_worker.py -q`，71 passed（16.98s）；相关联合：`.venv/bin/python -m pytest tests/hermes_cli/test_controlled_roots.py tests/hermes_cli/test_owner_runtime.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_web_server_fs.py tests/test_tui_gateway_owner_workspace_cwd.py -q`，100 passed（18.88s）；`git diff --check && .venv/bin/python -m ruff check hermes_cli/controlled_roots.py hermes_cli/owner_runtime.py hermes_cli/owner_worker/entrypoint.py tests/hermes_cli/test_controlled_roots.py tests/hermes_cli/test_owner_runtime.py tests/hermes_cli/test_owner_worker.py` passed。warning/首因：加入 `tests/tools/test_file_tools_cwd_resolution.py` 的联合命令为 125 passed、2 failed；最小复现为其两项写入测试在 macOS `/private/var/...` pytest tmp 路径命中既有 `_SENSITIVE_PATH_PREFIXES` 拒绝规则，属于未迁移 file-tools surface，未改生产或测试以越过 I7/1 范围。 |
| 2026-07-12 | I7 / 2 | 受控根现只接受相对路径；Linux 优先以 `openat2` 固定 `RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS|RESOLVE_NO_MAGICLINKS|RESOLVE_NO_XDEV` 打开，未知 binding 或 `ENOSYS` 才用 root-FD-relative `dir_fd` component walk；`O_NOFOLLOW`、对象类型、`st_dev`、不可继承和 FD cleanup 均 fail closed。非 Linux/能力不足不提供 pathname fallback；本步未迁移消费者。 | 修改：`hermes_cli/controlled_roots.py`、`tests/hermes_cli/test_controlled_roots.py`。focused：`.venv/bin/python -m pytest tests/hermes_cli/test_controlled_roots.py -q`，23 passed（0.82s）；相关联合：`.venv/bin/python -m pytest tests/hermes_cli/test_controlled_roots.py tests/hermes_cli/test_owner_runtime.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_web_server_fs.py tests/test_tui_gateway_owner_workspace_cwd.py -q`，118 passed（19.29s）；`git diff --check && .venv/bin/python -m ruff check hermes_cli/controlled_roots.py tests/hermes_cli/test_controlled_roots.py` passed。无 warnings；首个失败：directory expected-type 负例由内核 `O_DIRECTORY` 先返回 `ENOTDIR`，测试按正确的拒绝语义断言 `OSError`，未弱化生产协议。 |
| 2026-07-12 | I7 / 3 | authenticated files API 使用 runtime-bound descriptor workspace adapter，拒绝 ambient cwd/terminal fallback；受控读取有 10 MiB 上限，V4A Add/Update/Delete/Move 在预校验后执行，搜索仅遍历 owner workspace。checkpoint manager 以 descriptor-derived workspace identity 隔离 store ref/index，Git 子进程仅从继承目录 FD `fchdir`；authenticated preflight 不解析 tool path、终端 checkpoint 不猜测 cwd。logs、worker socket/log、temporary records、workspace/PTY/subprocess cwd 均经已有 controlled roots；未发现 authenticated archive import/extraction consumer。 | 修改：`tools/{file_operations,file_tools,checkpoint_manager}.py`、`agent/{agent_init,tool_executor}.py`、`hermes_cli/{authenticated_file_context,controlled_roots,logs,cli_commands_mixin}.py`、owner-worker/PTY 直接消费者及 focused tests。focused：file-tools 62 passed；checkpoint 81 passed；runtime consumers 141 passed。相关联合：`.venv/bin/python -m pytest tests/hermes_cli/test_controlled_roots.py tests/tools/test_file_tools.py tests/tools/test_file_tools_owner_workspace.py tests/tools/test_checkpoint_manager.py tests/hermes_cli/test_logs.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_pty_bridge.py tests/hermes_cli/test_owner_runtime.py tests/hermes_cli/test_web_server_files.py tests/test_tui_gateway_owner_workspace_cwd.py -q`，328 passed、11 existing PTY warnings（59.03s）。`.venv/bin/ruff check` modified filesystem modules 与 `git diff --check` passed。首因：legacy live-CWD test 的 macOS pytest temp 路径命中 `/private/var/` system guard；测试仅移除该 fixture prefix，生产 guard 未放宽。 |
| 2026-07-12 | I7 / 4 | bound owner-worker runtime 的 compatibility cwd 仅由 `AuthenticatedWorkspaceContext` 的 fixed `WORKSPACE/default` descriptor capability 选择；request/session/profile/env/persisted/project 路径只能精确回显该诊断 view，否则拒绝，缺 capability fail closed。completion/session/terminal registration/setter 与 project callback 使用同一入口；no-runtime 的 local 与 SSH compatibility branches 保持独立。 | 修改：`tui_gateway/server.py`、`tests/test_tui_gateway_owner_workspace_cwd.py`、`tests/tools/test_file_tools_cwd_resolution.py`。focused：owner CWD 16 passed；gateway CWD 9 passed；legacy terminal/file/runtime suites 68 passed。最终 `.venv/bin/python -m pytest -q tests/test_tui_gateway_owner_workspace_cwd.py tests/test_tui_gateway_server.py tests/tools/test_terminal_task_cwd.py tests/tools/test_file_tools_cwd_resolution.py tests/tools/test_file_tools_owner_workspace.py tests/hermes_cli/test_owner_runtime.py`，397 passed（63.52s）。`.venv/bin/ruff check tui_gateway/server.py tests/test_tui_gateway_owner_workspace_cwd.py tests/tools/test_file_tools_cwd_resolution.py` 与 `git diff --check` passed。首因：两项既有 legacy write/patch 断言在 macOS pytest `/private/var` temp 根触发系统路径 guard；仅在这两项测试移除该 fixture prefix，未放宽生产 guard。 |
| 2026-07-12 | I7 / 5 | checkpoint 默认路径不再由 import-time home 固定：无显式测试 override 时每次从 active runtime/profile home 解析；file operation 移除未使用 deny-list home cache；CLI clear 使用 `store_status()` 返回的 live base。 | 修改：`tools/checkpoint_manager.py`、`tools/file_operations.py`、`hermes_cli/checkpoints.py` 及 profile/write-deny/checkpoint CLI 单测。失败先验：profile A/B regression 在旧 equality sentinel 下返回 `None`（2 failed）；最小修复为 `CHECKPOINT_BASE is None` 动态分支。focused：33 passed、checkpoint CLI 82 passed、write/file 110 passed、owner runtime 54 passed；最终 `.venv/bin/python -m pytest -q tests/test_profile_isolation_runtime.py tests/tools/test_checkpoint_manager.py tests/tools/test_write_deny.py tests/tools/test_file_operations.py tests/hermes_cli/test_checkpoints.py tests/hermes_cli/test_owner_worker.py`，258 passed（45.20s）。Ruff 与 `git diff --check` passed；无 warnings。 |
| 2026-07-12 | I8 / 1 | authenticated Tool Executor 改为 fail-closed Linux Bubblewrap runtime：私有 user/mount/PID/IPC/network namespaces、private tmpfs root、fresh `/proc`/minimal `/dev` 与 `--die-with-parent`；workspace 只经已授权 directory FD `--bind-fd` 挂载为 `/workspace`，runtime 只挂当前 executor generation。child 复核 FD/mount device+inode 后关闭 FD；supervisor 跟踪 live invocation，并在 revoke/generation stop 后撤销 grants、终止精确匹配 sandbox process group、移除 registration。非 Linux、缺失/不可执行/不支持 `--bind-fd` 的 Bubblewrap 一律拒绝，不退回裸 `Popen`。 | 修改：`hermes_cli/owner_worker/{tool_executor_sandbox,tool_executor_supervisor}.py`、`hermes_cli/tool_executor_runtime/entrypoint.py`、`tests/hermes_cli/{test_tool_executor_sandbox,test_tool_executor_supervisor,test_tool_executor_runtime}.py`。focused：`.venv/bin/python -m pytest tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/tools/test_process_registry_executor_identity.py tests/hermes_cli/test_owner_worker.py`，75 passed（17.38s）；相关：`.venv/bin/python -m pytest tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/run_agent/test_tool_executor_contextvar_propagation.py tests/tools/test_process_registry.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py tests/tools/test_checkpoint_manager.py tests/test_tui_gateway_owner_workspace_cwd.py tests/tools/test_file_tools_owner_workspace.py`，300 passed、1 warning（55.56s）。warning：既有 PTY test 的 Python 3.12 `forkpty()` deprecation；`py_compile`、VS Code diagnostics 与 `git diff --check` passed。首个 blocker：裸 `subprocess.Popen` 不能建立 mount/PID/IPC namespace；最小复现为无 namespace child 仍共享 host mount/PID/IPC 视图；以 Bubblewrap fail-closed launcher 修复。 |
| 2026-07-12 | I8 / 2 | executor env 改为固定 sandbox-internal `/executor`、`/executor/tmp`，不再暴露 host owner/runtime/tmp 路径；workspace 仍只由受控 FD bind 为 `/workspace`，task/tool 参数、task/session/turn/request identity 只经一次性 bootstrap pipe 传递，不进入 env、Bubblewrap argv 或 cwd。env admission 要求 exact allowlist、非空 egress、三项 pairwise-distinct 且非 stdio FD；Bubblewrap 将 generation runtime 映射至 `/executor` 并固定从 `/workspace` 启动；child 复核 bootstrap egress 与 env 一致。 | 修改：`hermes_cli/tool_executor_runtime/{env,entrypoint}.py`、`hermes_cli/owner_worker/tool_executor_sandbox.py`、`tests/hermes_cli/{test_tool_executor_runtime,test_tool_executor_sandbox,test_tool_executor_supervisor}.py`。focused：`.venv/bin/python -m pytest tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py`，15 passed（1.29s）；相关：`.venv/bin/python -m pytest tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/run_agent/test_tool_executor_contextvar_propagation.py tests/tools/test_process_registry.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py tests/tools/test_checkpoint_manager.py tests/test_tui_gateway_owner_workspace_cwd.py tests/tools/test_file_tools_owner_workspace.py`，302 passed、1 warning（62.48s）。warning：既有 PTY test 的 Python 3.12 `forkpty()` deprecation；`py_compile`、VS Code diagnostics 与 `git diff --check` passed。首个缺口：child env 暴露 host runtime/tmp 绝对路径且未拒绝 duplicate/stdio FD；最小复现为 `build_executor_environment()` 将 host `runtime_home` 写入 `HOME`。改为 internal fixed paths 与 descriptor admission 后修复。 |
| 2026-07-12 | I8 / 3 | authenticated terminal descendants 由独立 fresh allowlist 构造，固定 `/executor` HOME/TMP 与受限 PATH/locale/Python safety；拒绝 Control Plane、owner-worker capability、dashboard ticket/token、replay/supervisor、Docker/SSH socket、executor bootstrap FD 与任意未知变量。`spawn_authenticated()` 保持 `close_fds=True`、`stdin=DEVNULL` 且不传 `pass_fds`；terminal authenticated branch 不再转发 `env.env`。I8/1 的 Bubblewrap private PID/IPC namespaces 继续作为前置隔离契约。 | 修改：`hermes_cli/tool_executor_runtime/env.py`、`tools/{process_registry,terminal_tool}.py`、`tests/tools/{test_process_registry_executor_identity,test_terminal_task_cwd}.py`。focused：`.venv/bin/python -m pytest tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py tests/hermes_cli/test_tool_executor_runtime.py`，22 passed（1.16s）；相关：`.venv/bin/python -m pytest tests/tools/test_process_registry.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_owner_worker.py`，179 passed（27.51s）。`py_compile`、VS Code diagnostics 与 `git diff --check` passed；无 warnings。首个失败：新增拒绝测试缺少 `pytest` import，最小复现为 `NameError: name 'pytest' is not defined`；补充测试 import 后通过，未放宽生产边界。 |
| 2026-07-12 | I8 / 4 | owner-local credential broker 仅保存 opaque、1–300 秒的 exact task/audience/operation/scope/worker-generation/executor-generation grant；claims 快照 identity fences，拒绝空、control-character、wildcard 或 aggregate metadata，并在 `expires_at` 边界失效。exact executor revoke 与 exact owner/worker/generation stop 原子删除现有 grants、阻止未来 matching grant；shutdown 在 stop generation 后清空 transient broker state。 | 修改：`hermes_cli/owner_worker/{executor_tokens,credential_broker,entrypoint}.py`、`tests/hermes_cli/{test_credential_broker,test_tool_executor_supervisor}.py`。focused：`.venv/bin/python -m pytest tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_tool_executor_supervisor.py`，19 passed（1.20s）；相关：`.venv/bin/python -m pytest tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_owner_worker.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，99 passed（16.99s）。`py_compile`、VS Code diagnostics 与 `git diff --check` passed；无 warnings。首个失败：claim snapshot test 在未固定 `now` 时使用 wall clock，先触发 expiry 而非 identity mismatch；最小修复为在 direct-claim assertion 传 `now=100`，未放宽生产 expiry gate。 |
| 2026-07-12 | I8 / 5 | authenticated `ProcessSession` 以完整 immutable `ExecutorIdentity` 作为唯一权威 fence，stdin submit/close、terminal close、poll/log/wait/kill 与 executor dispatch 均要求 exact identity（含 lease/recovery generation）；checkpoint 仅以 `to_payload()`/`from_payload()` 持久化并恢复 authenticated identity，缺失、损坏或 task/session mismatch record 一律跳过而不降级为 legacy。completion/watch queue 和 terminal output/close multiplex 仅对 matching owner/worker/worker-generation/lease/recovery runtime 交付 authenticated provenance；legacy routing 不变。supervisor revoke/generation stop 额外精确 reaps registry descendants，且不改变 broker return value。 | 修改：`tools/{process_registry,close_terminal_tool}.py`、`tui_gateway/server.py`、`hermes_cli/owner_worker/tool_executor_supervisor.py`、`tests/{test_tui_gateway_server.py,tools/test_process_registry.py,tools/test_process_registry_executor_identity.py,hermes_cli/test_tool_executor_supervisor.py}`。focused：`.venv/bin/python -m pytest tests/tools/test_process_registry_executor_identity.py tests/tools/test_process_registry.py tests/test_tui_gateway_server.py tests/hermes_cli/test_tool_executor_supervisor.py -q`，434 passed、1 warning（85.63s）；相关：`.venv/bin/python -m pytest tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_owner_worker.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_process_registry.py tests/tools/test_terminal_task_cwd.py tests/test_tui_gateway_server.py -q`，525 passed、1 warning（78.43s）。warning：既有 PTY test 的 Python 3.12 `forkpty()` deprecation；`py_compile`、VS Code diagnostics 与 `git diff --check` passed。首个失败：new live-output routing test shared `session-key` with an existing TUI fixture, causing owner resolution to select `sid` instead of the intended test session；最小复现为 assertion expected `sid-auth-output` but observed `sid`，修复为独立 session key，未放宽 production route fence。 |
| 2026-07-12 | I9 / 1 | 每次 authenticated Tool Executor dispatch 现在以 immutable `SandboxLaunchBinding` 精确绑定 trusted `ExecutorIdentity`、owner/worker/lease/recovery/executor generation、canonical owner runtime 与 fresh sandbox instance；每次 invocation 的 mount view、tmpfs 与 security-subject identity 均不同。supervisor 在创建目录、FD、pipe 或 `Popen` 前验证 identity 与当前 lease 完全一致；foreign/stale identity 不启动 child。Bubblewrap builder 只接受与 binding 完全一致的 owner/runtime 路径，workspace 仍仅以 `--bind-fd` 进入 sandbox。 | 修改：`hermes_cli/owner_worker/{tool_executor_sandbox,tool_executor_supervisor}.py`、`tests/hermes_cli/{test_tool_executor_sandbox,test_tool_executor_supervisor}.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py`，17 passed（0.95s）；相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，113 passed（19.22s）。`.venv/bin/python -m py_compile`（modified files）与 `git diff --check` passed。首个失败：binding-path negative test 传入不存在的 alternate owner，先触发正确的 canonical-path unavailable 拒绝而未匹配预期文字；最小修复为创建该 alternate directory，使测试精确验证 binding mismatch，未放宽 production gate。 |
| 2026-07-12 | I9 / 2 | authenticated sandbox launch 现在强制要求 immutable、host-observed `SandboxVerificationRecord`；它以 schema/provenance/time window、image digest/profile、exact owner/worker/lease/recovery/executor/sandbox identity、non-root UID/GID、mount/tmpfs/security-subject IDs、network/egress、readonly rootfs/no-new-privileges/capability drop/namespaces 与 frozen policy 全等验证。source 缺失、返回空、异常、陈旧或任一不匹配均在创建目录、workspace FD、pipe、env/spec 或 `Popen` 前 fail closed；record 只保留在 owner-side live invocation，绝不进入 child env/argv/bootstrap。 | 修改：`hermes_cli/owner_worker/{tool_executor_sandbox,tool_executor_supervisor}.py`、`tests/hermes_cli/{test_tool_executor_sandbox,test_tool_executor_supervisor}.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py`，44 passed（1.57s）；相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，133 passed（22.95s）。`.venv/bin/python -m py_compile`（modified files）与 `git diff --check` passed。无 failures/warnings。 |
| 2026-07-13 | I9 / 3 | sandbox mount topology 现在由 immutable `SandboxMountPolicy` 统一生成：唯一 writable host bind 为当前 binding 的 exact executor-generation runtime `/executor`；global resources 必须显式 canonical allowlist 并仅以 `--ro-bind` 进入；workspace 只由 `--bind-fd` 进入。policy 拒绝 owner/workspace/control overlap、root、host `/proc`/`/sys`/`/dev`/`/run`/`/var/run` 等危险 source，且 `mount_policy_id` 被 host verification record 精确提交。根与 `/executor/tmp` 均由 bounded `--size ... --tmpfs` 生成，后者 overlay host runtime tmp；supervisor 不再创建 host-backed executor tmp。source/evidence mismatch 在 mkdir、FD、pipe/env/spec/Popen 前拒绝。 | 修改：`hermes_cli/owner_worker/{tool_executor_sandbox,tool_executor_supervisor}.py`、`hermes_cli/tool_executor_runtime/env.py`、`tests/hermes_cli/{test_tool_executor_sandbox,test_tool_executor_supervisor,test_tool_executor_runtime}.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py`，46 passed（1.60s）；相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，135 passed（23.06s）。`.venv/bin/python -m py_compile`（modified files）与 `git diff --check` passed。首个失败：policy 以 lexical `/proc` 规则遇到 macOS resolve 不存在；最小修复为先按 requested path 拒绝敏感树、再 canonicalize allowed roots，未放宽 production mount gate。 |
| 2026-07-13 | I9 / 4 | immutable `SandboxSecurityPolicy` 现将 profile、backend、non-root UID/GID、readonly rootfs、no-new-privileges、empty capability contract 与 policy-ID/digest 绑定；host record 必须逐项提交。Bubblewrap 缺少 `--uid`、`--gid`、`--cap-drop` 或 `--seccomp` 即拒绝；成功 spec 显式 request non-root UID/GID、`--cap-drop ALL` 与 parent-owned policy-bound seccomp FD。supervisor 在 mkdir/FD/pipe/env/spec/Popen 前验证并复制 trusted filter FD，且只将该 FD 通过 `pass_fds` 交给 Bubblewrap，随后关闭 parent copy；record、filter path/bytes 不进入 child env/bootstrap。 | 修改：`hermes_cli/owner_worker/{tool_executor_sandbox,tool_executor_supervisor}.py`、`tests/hermes_cli/{test_tool_executor_sandbox,test_tool_executor_supervisor}.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py`，59 passed（1.84s）；相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，148 passed（19.11s）。`py_compile`、VS Code diagnostics 与 `git diff --check` passed。首个失败：supervisor initially omitted the copied seccomp FD from `pass_fds` (focused 1 failed); include the exact inherited FD only, without exposing it to child payload/env. |
| 2026-07-13 | I9 / 5 | egress profile 现为封闭 canonical vocabulary；supervisor 仅以 immutable owner-side tool-name policy 选择 profile，且 Tool Executor 只允许 `tool-none`、`tool-public`、`protected-target`。`control-only`、`owner-public`、unknown/malformed/noncanonical 值均在 launch preparation 前拒绝；host verification、env 与 bootstrap 只接受并精确比对 canonical executor profile，缺失 bootstrap profile 不再回退。未启用网络、target resolution 或 Bubblewrap network 行为。 | 修改：`hermes_cli/owner_worker/{executor_identity,tool_executor_supervisor,tool_executor_sandbox}.py`、`hermes_cli/tool_executor_runtime/{env,entrypoint}.py`、`tests/hermes_cli/{test_tool_executor_sandbox,test_tool_executor_supervisor,test_tool_executor_runtime}.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py`，90 passed（2.53s）；相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/run_agent/test_tool_executor_contextvar_propagation.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，184 passed（24.15s）。`py_compile`、VS Code diagnostics 与 `git diff --check` passed；无 warnings。首个失败：新 profile-selection test 错附 `verification_source` 参数化 decorator，collection 报 function uses no argument；将 decorator 复位到 verification-source test 后通过，未放宽 production gate。 |
| 2026-07-13 | I10 / 1 | immutable `ExecutorResourceQuota`/`ExecutorResourceDecision` 现在完整覆盖 worker、CPU、memory、pids、disk/inode、FD、duration、concurrent tool、PTY、WS、Gateway channel、output 与 network bytes/requests；decision 由 exact owner/workspace/worker/lease/recovery/executor identity 导出 stable policy ID。supervisor 在 resource decision 缺失、不可用、foreign 或 mismatch 时先于 workspace FD、runtime directory 或 spawn 拒绝；bootstrap 必须携带并复核 canonical policy ID/quota，不能回退 default/global policy。 | 修改：`hermes_cli/owner_worker/{executor_identity,tool_executor_supervisor}.py`、`hermes_cli/tool_executor_runtime/entrypoint.py`、`tests/hermes_cli/{test_tool_executor_supervisor,test_tool_executor_runtime}.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_runtime.py tests/tools/test_process_registry_executor_identity.py`，102 passed（3.75s）。相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_owner_worker.py tests/run_agent/test_tool_executor_contextvar_propagation.py tests/tools/test_process_registry.py tests/tools/test_process_registry_executor_identity.py tests/tools/test_terminal_task_cwd.py`，293 passed、1 existing `forkpty()` deprecation warning（41.05s）。`py_compile`、targeted Ruff 与 `git diff --check` passed。首个失败：无。 |
| 2026-07-13 | I10 / 2 | owner-worker startup throttle 现保持 per-owner；`max_owner_concurrency` 在 exact active lease/handle 后、计数前 fail closed。capacity pressure 现在只驱逐 live idle handle 中最久未使用者，recent idle 也可驱逐；active handle 不可驱逐，capacity 满且全 active 时在 authority claim/spawn 前拒绝。evicted A 经既有 exact lease drain/bridge close/termination 后，B 只以自己的 home/socket/new generation cold start，绝不复用 A runtime。 | 修改：`hermes_cli/owner_worker/supervisor.py`、`tests/hermes_cli/test_owner_worker.py`。focused：`.venv/bin/python -m pytest -q tests/hermes_cli/test_owner_worker.py -k 'supervisor and (throttle or concurrency or evict or capacity or idle)'`，6 passed、52 deselected（1.29s）。相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py`，525 passed、10 existing warnings（107.09s）。`py_compile`、targeted Ruff 与 `git diff --check` passed。首个失败：无。 |
| 2026-07-14 | I10 / 3 | 补齐去敏 lifecycle audit 的实际边界与 fail-closed 证据：bridge 的 exact durable fence 在 worker change 已观察后禁止迟到注册；PTY 仅在 spawn/attach 成功后记录 `ADMITTED`，现有 finally 清理路径仅记录一次 `BRIDGE_CLOSED`；lifecycle reporter 只传 server-generated correlation ID、固定 audience、trusted worker generation，且 audit failure 不阻断 cleanup。authenticated worker 仅从显式 operator factory 获得 deployment policy；缺失配置使 executor unavailable，不回退 registry/default source。 | 修改：`hermes_cli/{owner_runtime.py,owner_worker/supervisor.py}`、`tests/hermes_cli/{test_dashboard_auth_audit.py,test_dashboard_auth_ws_auth.py,test_owner_worker.py}`。新增 focused：bridge tombstone 2 passed；entrypoint policy/PTY lifecycle 3 passed；lifecycle audit reporter 1 passed。相关：`.venv/bin/python -m pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_owner_worker_ws_bridge.py tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_credential_broker.py tests/hermes_cli/test_web_server.py tests/test_model_tools.py`，700 passed、10 existing warnings（104.47s）。`.venv/bin/python -m py_compile`、Ruff 与 `git diff --check` passed。首个失败：测试将 `HERMES_SANDBOX_DEPLOYMENT_POLICY` 直接设入 worker env，严格 owner env allowlist 正确拒绝；最小修复为显式 allowlist 并在 supervisor child env 中保留该 operator-only factory spec，未开放其他 `HERMES_*` 变量。未实现或声称 numeric runtime quota enforcement、active egress enforcement 或 FIFO fairness。 |
| 2026-07-13 | I10 / 4 | A/B matrix 现覆盖 verified HTTP session 到 independent exact owner handle、authenticated Control Plane filesystem/checkpoint 403、scope/epoch 与 exact worker-generation bridge revoke、owner-local PTY active-session/Gateway attach state、descriptor workspace/checkpoint preservation、executor credential/process revoke，以及 per-owner cooldown/concurrency/idle-only capacity semantics。A 的拒绝、revoke 或 cleanup 不会路由、释放、读取、修改或终止 B；未声称 I10/1 的数值 quota enforcement，也未声称 FIFO fairness。 | 修改：`tests/hermes_cli/test_web_server.py`、`tests/hermes_cli/test_owner_worker.py`、`tests/hermes_cli/test_credential_broker.py`、`tests/tools/test_file_tools_owner_workspace.py`。focused：`source /Users/weixiangbin/work/hermescopy/.venv/bin/activate && pytest -q tests/hermes_cli/test_web_server.py tests/hermes_cli/test_web_server_fs.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_owner_worker_ws_bridge.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/tools/test_file_tools_owner_workspace.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_credential_broker.py`，581 passed、10 existing warnings（97.33s）。相关：`source /Users/weixiangbin/work/hermescopy/.venv/bin/activate && pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_web_server.py tests/hermes_cli/test_web_server_files.py tests/hermes_cli/test_web_server_fs.py tests/hermes_cli/test_web_server_pty_reconnect.py tests/hermes_cli/test_owner_worker.py tests/hermes_cli/test_owner_worker_ws_bridge.py tests/tools/test_file_tools_owner_workspace.py tests/hermes_cli/test_tool_executor_supervisor.py tests/hermes_cli/test_tool_executor_runtime.py tests/hermes_cli/test_tool_executor_sandbox.py tests/hermes_cli/test_credential_broker.py`，739 passed、10 existing warnings（98.94s）。`py_compile`、targeted Ruff 与 `git diff --check` passed。首个失败：无。 |
| 2026-07-13 | I10 / 5 | local token/legacy session routes 仍只使用其 local profile-backed `SessionDB`，不会创建 Owner Worker capability/request；authenticated session、analytics 与 model-info 在 worker request 失败时返回 502，绝不回退 `SessionDB`、profile home、config 或 global helper。authenticated legacy owner selectors 在 proxy/fallback 前拒绝；explicit route allowlist 继续拒绝 unknown session subpath、adjacent analytics/model、profiles/checkpoints 和 global-management bypass。 | 修改：`tests/hermes_cli/test_web_server.py`、`tests/hermes_cli/test_dashboard_auth_middleware.py`。focused：`source /Users/weixiangbin/work/hermescopy/.venv/bin/activate && pytest -q tests/hermes_cli/test_web_server.py::TestAuthenticatedOwnerWorkerSessionProxy tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_owner_runtime.py`，110 passed、1 existing `audioop` deprecation warning（6.73s）。相关：`source /Users/weixiangbin/work/hermescopy/.venv/bin/activate && pytest -q tests/hermes_cli/test_web_server.py tests/hermes_cli/test_web_server_session_search.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_owner_runtime.py tests/hermes_cli/test_owner_worker.py tests/tools/test_file_tools_owner_workspace.py tests/test_tui_gateway_owner_workspace_cwd.py`，540 passed、10 existing warnings（90.32s）。`py_compile`、targeted Ruff 与 `git diff --check` passed。首个失败：无。 |


## 归档：已关闭迭代

<details>
<summary>迭代 3：Worker generation 和 owner-bound 控制握手（已完成，2026-07-11，6/6）</summary>

## 迭代 3：Worker generation 和 owner-bound 控制握手

> **执行状态：** `已完成`（2026-07-11） · **步骤：** `6/6` · **恢复入口：** 迭代 3 已关闭；严格从迭代 4 / 步骤 1 开始。

**目标：** 将 Owner Runtime Worker 变为可验证、可撤销、不可跨 owner 复用的 peer。

- [x] 每次 worker 启动分配单调 `worker_generation`，建立 `starting → active → draining/revoked → terminated/failed` 生命周期。`AuthorityStore` v2、supervisor generation socket 与 health identity 已闭环；证据见索引 I3/1。
- [x] 用 authority-backed lease/CAS 处理同 owner 并发启动、crash、replace 和 idle recycle，避免 stale worker resurrection。schema v3 的 exact lease/recovery fence 与 lifecycle CAS 已闭环；证据见索引 I3/2。
- [x] 内部 capability 精确绑定 issuer/key version、owner identity、worker ID/generation、audience、scope、protocol version、短 expiry 和 `jti`。`owc1` signed capability、active/retained public verifier 与 legacy fail-closed 已闭环；证据见索引 I3/3。
- [x] bootstrap capability 在 control-only replay store 单次消费；握手后连接绑定 peer identity、随机 nonce 和单调序号，拒绝跨连接重放与序号回退。`owp1` bootstrap/hello/ack/data framing 已闭环；证据见索引 I3/4。
- [x] Worker 在 owner-sensitive import 前固定 `HERMES_HOME=runtime_owner_home`，只接收最小 owner 环境，并自检 DB、runtime、logs、checkpoint、session index 与 workspace 都位于 runtime root。`OwnerWorkerRuntimePaths`、exact resolver checks 与 poisoned env rejection 已闭环；证据见索引 I3/5。
- [x] replace/revoke 时先以 exact lease transition 进入 `DRAINING`，使旧 capability/bootstrap 不可接入；同步关闭 exact-generation browser bridge，再终止旧进程并仅清理其 generation socket。已确认退出才写入 `TERMINATED`，stale cleanup 不得影响 replacement；证据见索引 I3/6。

**完成条件：** A/B 不共用 active generation；重启/PID reuse 后旧 health、token、route、PTY attach 和 IPC 均失败；owner/home/workspace/generation 任一错配不能接入。

</details>

<details>
<summary>迭代 1：统一 OwnerContext 和 owner home（已完成，2026-07-10，6/6）</summary>

## 迭代 1：统一 OwnerContext 和 owner home

> **执行状态：** `已完成`（更新日期：2026-07-10；步骤 1–6、AF_UNIX、WS auth 和完整 web-server regression 均已收敛） · **步骤：** `6/6` · **代码状态：** canonical identity、keyring/rotation、统一派生、host/runtime path contract、Control Plane host-home admission、authenticated profile boundary 与 owner-bound WS capability 已闭环 · **单元测试：** focused web server：25 passed（11.94s）；完整 web server：358 passed（73.64s）；auth/WS/web server 联合回归：454 passed（71.59s）  
> **恢复入口：** 迭代 1 已关闭。迭代 2 仍未开始；仅在收到后续明确执行指令后，严格从迭代 2 的第一个未勾选条目开始。

**目标：** 固定身份、命名和目录选择，避免 HTTP、WS、worker spawn 对同一用户得出不同 owner。

- [x] 提供唯一的不可变 `OwnerContext` 派生模块，固定 `auth_provider + tenant_id + owner_user_id` 的 canonical 规则。（2026-07-10：复用 `hermes_cli/dashboard_auth/owner_context.py` 的 frozen `OwnerContext`、`owner_context_from_session()`、`owner_context_from_ticket_payload()`；在 `tests/hermes_cli/test_dashboard_auth_ws_tickets.py::TestOwnerContext` 增加 A/B principal、不可变性、缺失可信 user ID、HTTP/WS/worker 一致性和 ticket owner mismatch 负例。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py -k TestOwnerContext`，12 passed、19 deselected，2.33s。）
- [x] 使用带版本前缀的 HMAC 生成不可预测、文件系统安全的 `owner_key`；定义 secret 持久化、备份和显式轮换迁移。（2026-07-10：保留 `ok1_` 兼容 HMAC，新增私有 `control-plane/owner_keyring.json` 的 active/retained/pending metadata、`owner_keyring_backup_paths()` 和显式 begin → migrate → complete 流程；环境 active-secret 不一致、空/冲突 pending secret、迁移目标冲突与未完成迁移均 fail closed。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py -k 'rotation or keyring'`，4 passed、31 deselected，0.46s；完整 `TestOwnerContext` 16 passed、19 deselected，0.60s。）
- [x] 让 HTTP session、`/api/auth/me`、WS ticket、worker spawn 和 server-spawn context 使用同一派生函数。（2026-07-10：`routes.py` 的 auth-me/ticket 和 `web_server.py` 的 authenticated proxy 复用 `owner_context_from_session()`；WS 使用 `owner_context_from_ticket_payload()` 重新验证 ticket principal；`OwnerWorkerSupervisor` 将 canonical owner 的 key/home/provider/tenant/user 传给 argv/env。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_owner_worker.py -k 'canonical_owner or distinct_users_mint or canonical_session_owner_context'`，3 passed、81 deselected，1.56s。）
- [x] 固定术语与职责：`host_global_home`、`host_owner_home`、`runtime_owner_home`。容器路径、容器 ID、环境变量或前端参数不能证明身份。（2026-07-10：`OwnerContext` 固定 host-side 路径并保留 `owner_home` 兼容 alias；`owner_runtime.get_runtime_owner_home()` 只读取 worker `HERMES_HOME`，`assert_owner_runtime_paths()` 以 runtime root 做自检。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_owner_runtime.py -k 'same_verified_session or runtime_owner_home or assert_owner_runtime_paths'`，3 passed、41 deselected，0.48s；`tests/hermes_cli/test_owner_worker.py -k 'canonical_session_owner_context or supervisor_rejects_same_key'`，2 passed、20 deselected，0.53s。）
- [x] 由 Control Plane 从 `OwnerContext` 安全创建/确认 `host_owner_home`；拒绝 symlink、异常父目录、错误权限、非预期 inode/mount。（2026-07-10：`_admit_host_owner_home()` 从 canonical host global/users/owner key 构造路径，lstat 拒绝 symlink/非目录，拒绝 group/world writable/错误 ownership home，检查 users/owner device 一致并在创建后复核 inode/type；新增 users symlink 与 unsafe existing mode 负例。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py -k TestOwnerContext`，18 passed、19 deselected，0.61s。）
- [x] authenticated 模式拒绝外部 `profile`，或仅映射到 owner-local 子命名空间，绝不能选择任意 DB/home。（2026-07-10：Control Plane proxy 在启动 worker 前拒绝 query `owner`、`owner_home`、`owner_key` 及非兼容 `profile`，只移除兼容 `default/current`；worker HTTP middleware 同样拒绝外部 owner selector，worker route 继续拒绝 legacy profile。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_web_server.py tests/hermes_cli/test_owner_worker.py -k 'authenticated_owner_key_query or authenticated_profile_selection or authenticated_templated_session_routes or worker_routes_reject_external_owner_selector or worker_session_routes_reject_legacy_profile_selection or worker_analytics_routes_reject_legacy_profile_selection'`，8 passed、373 deselected，2.06s。）
- [x] 处理迭代 1 相关 unit regression blocker：`tests/hermes_cli/test_owner_worker.py::test_worker_health_over_unix_socket_reports_owner_env` 在当前 pytest temp 根下生成超过 AF_UNIX 长度限制的 socket path。（2026-07-10：用户已明确许可修复/验证此多进程 unit regression；测试改为短的 per-process `/tmp/h<pid>` socket root，并向 child 显式传入匹配的 `HERMES_HOME`、`HERMES_OWNER_KEY`。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_owner_worker.py`，23 passed，9.01s。）
- [x] 收敛迭代 1 关闭前 WS auth unit regression：`tests/hermes_cli/test_dashboard_auth_ws_auth.py` 仍有 5 项旧断言要求 authenticated gated mode 使用 process-global `?internal=` credential 或构建携带它的 sidecar/gateway URL；这与当前 owner-bound internal capability 设计不一致。先新增/调整 A/B 失败语义覆盖，再按同一 capability 契约实现或移除旧行为，并运行完整相关 unit regression。（2026-07-10：将 gated mode 的 process-global credential 成功/复用断言改为 `internal_owner_context_required` 拒绝；将 Control Plane gated sidecar/gateway URL 断言改为 `None`；新增 worker-mode URL 仅含 `internal_owner_token`、绑定 owner/audience/exact path 且 A/B payload 不同的覆盖。生产边界已符合契约，无需修改 `web_server.py`。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_auth.py`，62 passed，2.63s。）
- [x] 收敛迭代 1 关闭前完整 web-server unit regression：将 plugin manifest 无认证旧预期改为 `401`（保持最小 public allowlist）；将 local `/api/model/info` 测试显式置于 loopback token contract，authenticated 请求仍代理 Owner Worker；将 PTY resolver doubles 更新为接受并验证 `browser_id`、`app_obj` 的 app/browser-aware 契约，未恢复任何 authenticated legacy/global fallback。（2026-07-10：focused `test_web_server.py -k 'test_unauthenticated_api_blocked or TestModelInfoEndpoint or TestPtyWebSocket'`：25 passed、333 deselected，11.94s；完整 `test_web_server.py`：358 passed，73.64s；middleware/WS auth/web-server 联合回归：454 passed，71.59s。）

**完成条件：** 同一登录态在 HTTP、WS 和 worker spawn 得到同一 owner key；owner 缺失/不一致即失败；重启后 owner key 稳定；A 不能通过 owner/profile/path 参数选择 B 的目录。


</details>

<details>
<summary>迭代 2：共享授权状态、epoch 和 WS ticket 撤销（已完成，2026-07-10，6/6）</summary>

## 迭代 2：共享授权状态、epoch 和 WS ticket 撤销

> **执行状态：** `已完成`（更新日期：2026-07-10） · **步骤：** `6/6` · **代码状态：** shared authority/replay store、signed ticket、bridge revocation、key rotation/recovery continuity 与 Control-Plane de-identified audit 已闭环 · **单元测试：** authority/ticket/audit/WS/middleware/web-server：528 passed、10 warnings（93.99s）  
> **恢复入口：** 迭代 2 已关闭；严格从迭代 3 的第一个未勾选条目开始。

**目标：** 把 browser WS ticket 从进程内状态升级为跨 Control Plane 副本可原子消费和撤销的授权能力。

- [x] 建立 control-only shared authority/replay store，持久保存 authorization epoch、session/membership revoke、ticket consume/replay 和内部 capability revoke 元数据。（2026-07-10：新增 `hermes_cli/dashboard_auth/authority.py`，以 `control-plane/authority.sqlite3` 持久化 scope epoch、session revoke、membership revision、exact-audience credential replay key 和 recovery generation；SQLite `BEGIN IMMEDIATE`、symlink/权限/schema/transaction fail-closed 已覆盖。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py`，8 passed，0.52s。）
- [x] 实现原子操作：读取当前 epoch、revoke 并递增 epoch、exact-audience `jti` check-and-consume-until-expiry，以及恢复不可信时 outstanding credential 全部失效。（2026-07-10：`AuthorityStore.activate()`/`read_state()` 提供 epoch 读取；`revoke_and_bump()` 使 verified session scope 失效并递增 epoch；`check_and_consume()` 在 `BEGIN IMMEDIATE` transaction 内验证 epoch/recovery generation、清理过期记录并插入 `(token_class, issuer_key_version, jti digest, audience)` 唯一 replay key；`invalidate_outstanding_credentials()` 递增 recovery generation。A/B、竞争消费、expiry、exact audience 和 recovery mismatch 的单元测试已覆盖。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py`，8 passed，0.63s。）
- [x] browser WS ticket 只包含可重建 `OwnerContext` 的可信 principal 材料、issuer/key version、exact public route/protocol audience、短 TTL、epoch 和唯一 `jti`。（2026-07-10：`ws_tickets.py` 改为签名 `bwt1` capability，claims 包含 provider/org/tenant/user/owner_key、issuer key version、`jti`、`iat`/`exp`（30 秒）、authority epoch/recovery generation 与受限 `browser-ws:/api/{pty,ws,pub,events}` audience；不再使用进程内 browser ticket 字典。`verify_ticket()` 在未消费前验证签名、version、issuer、TTL、claims 和 exact audience，`consume_ticket()` 委托 authority store 的原子消费。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_authority.py`，47 passed，1.84s。）
- [x] Control Plane 在 WS upgrade 前验证 session/成员资格/epoch/audience，并在 shared replay store 中原子单次消费；ticket 永不转发 Worker。（2026-07-10：`middleware.verify_websocket_ticket_session()` 在 WS upgrade 中用 ticket 绑定 provider 重新验证 HttpOnly access-session cookie，交叉核对 provider/user/org、canonical OwnerContext 与 provider `authorization_state()` 的 session handle/membership revision；`_ws_auth_result()` 先验证签名和 exact `browser-ws:<ws.url.path>` audience、重建 owner，再执行该 current-session/membership 校验，最后才调用 authority store 原子 consume，因此失败 upgrade 不会烧毁 ticket。provider outage 返回 fail-closed `authority_unavailable`，其余认证失败发生在 accept/supervisor/bridge 前；既有 `_ws_query_for_owner_worker()` 持续剥离 browser ticket。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py`，505 passed、10 warnings，81.82s。)
- [x] logout、session expiry/revoke、tenant 切换和成员资格/角色变更撤销受影响 ticket 并关闭旧连接；无可信 session 的 logout 幂等且不影响其他 owner。（2026-07-10：`AuthorityStore` 追加跨 Control Plane replica 可读取的仅含 scope digest/epoch/revoked 的顺序变更记录；`web_server.py` dispatcher 按变更关闭本地匹配 bridge，authority 读取失败时 fail-close 关闭本地已认证 bridge。logout 只在 fresh verified session 后 revoke，provider outage/no session 不创建 wildcard revoke；HTTP/refresh authority activation 对 tenant/session/membership transition 失效旧 ticket 并关闭旧 epoch bridge；browser access-session expiry 关闭浏览器 half，bridge `finally` 关闭 worker half 并释放 lease。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_dashboard_auth_ws_auth.py`，119 passed、1 warning，5.77s；完整相关单元回归 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py`，516 passed、10 warnings，87.40s。）
- [x] 定义 key rotation、authority outage、replay-store 恢复失败的 fail-closed 和去敏审计行为。（2026-07-10：browser ticket 使用独立 Control-Plane keyring 的 active/retained/pending state machine，rotation 仅接受 active signing、在 30-second TTL bounded window 内以 retained issuer 验证旧 ticket，未知/过期 issuer、pending/metadata/path failure 均拒绝；worker 环境必须显式提供 `HERMES_CONTROL_HOME`，不能写入 owner-local authority state。authority SQLite/keyring witness 同时证明 replay continuity，store/keyring replacement、不可用或 recovery-required 阻断 mint/verify/consume；显式 recovery 推进 generation，令旧未消费 ticket 失效，只允许新 ticket。authority audit 写入 Control Plane `logs/authority.log`，每条记录有 server-generated correlation ID，并且仅允许去敏 reason/audience-class/epoch/generation/digest 字段；HTTP mint 与 WS admission 的 outage 均 fail closed，WS 不记录 raw exception/ticket。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_authority.py`，62 passed，2.18s；完整相关单元回归 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py`，528 passed、10 warnings，93.99s。）

**完成条件：** A logout 后任一副本都拒绝 A 已签发未消费 ticket，B 不受影响；竞争消费只能成功一次；过期、错误签名、错误 route/audience、重复 jti、store 不可用均拒绝；重新登录的新 ticket 成功。


</details>

<details>
<summary>迭代 4：将 session API 迁入 Owner Worker（已完成，2026-07-11，5/5）</summary>

## 迭代 4：将 session API 迁入 Owner Worker

> **执行状态：** `已完成`（2026-07-11） · **步骤：** `5/5` · **恢复入口：** 迭代 4 已关闭；严格从迭代 5 / 步骤 1 开始。

**目标：** Control Plane 只认证、路由和透传，绝不直接读取 authenticated owner 的数据或恢复状态。

- [x] 先迁移只读 API：list、search、detail、messages、stats、export、latest-descendant/lineage。（2026-07-11：复用现有 `session_api.py` payload helpers、Owner Worker 对应路由和 Control Plane `_proxy_authenticated_owner_http()`；扩展参数化 A/B 负例，断言所有已列只读 route 只在 Worker 读取 owner-local DB，Control Plane 不打开 `SessionDB` 或调用本地 lineage helper，`/api/profiles/sessions` 保持 403。验证与首因见证据索引 I4/1。）
- [x] 再迁移写 API：rename/patch、delete、prune 和 resume 相关写入。（2026-07-11：认证写 surface 继续复用 `_proxy_authenticated_owner_http()` 与 Worker `session_api.py` helpers；将 Worker 请求 body model 提升为模块级，确保 FastAPI 在 postponed annotations 下按 JSON body 解析。参数化 A/B 负例证明 PATCH、single/bulk delete、empty cleanup、prune 仅改变 owner-local DB，global sentinel 不受影响；resume read/lineage 维持步骤 1 的 Worker-local resolve。验证与首因见证据索引 I4/2。）
- [x] Control Plane 从可信 session 重新派生 OwnerContext，只代理到健康且 generation 匹配的 worker，并携带内部 capability。（2026-07-11：`_proxy_authenticated_owner_http()` 保持 `owner_context_from_session()` 和 Supervisor `get_or_start()`/`acquire_use()` 的 active lease gate；增加 handle owner 与可信派生 owner 的显式一致性检查，错配时在 capability/request 前 fail closed。正常路径验证 capability 精确包含 handle 的 owner/generation/worker ID/lease/recovery fence，并释放 use lease。验证与首因见证据索引 I4/3。）
- [x] 禁止 authenticated Control Plane 直连 `SessionDB`、global DB、profile 聚合和默认 home fallback；未迁移 surface 返回 403/404。（2026-07-11：`_open_session_db_for_profile()` 增加 authenticated mode fail-closed guard，阻止未来已认证路由意外落入 Control Plane/global 默认 DB；`/api/profiles/sessions` 保持 403，`api_availability` 继续枚举 Worker 路由而非 prefix allow，未迁移 surface 保持 403。验证见证据索引 I4/4。）
- [x] session/title/short ID/resume ID 的查找必须在 worker owner scope 内完成；scope 外统一返回 404 或空结果。（2026-07-11：新增 A/B Worker test，验证 B 的 short ID 对 A 的 detail/messages/export/latest-descendant 全部 404，B title 搜索为空；A 的 short ID 仅解析为 A 自己的完整 session ID。验证见证据索引 I4/5。）

**完成条件：** A/B 使用独立 owner-local `state.db`；authenticated 请求不读取 global DB；A 无法 list/search/detail/export/update/delete/prune/resume B 的 session；legacy 路径保持显式独立。已满足；最终相关回归 490 passed、9 warnings（90.89s）。

</details>

<details>
<summary>迭代 5：WS、PTY 和浏览器连接边界（已完成，2026-07-11，4/4）</summary>

## 迭代 5：WS、PTY 和浏览器连接边界

> **执行状态：** `已完成`（2026-07-11） · **步骤：** `4/4` · **恢复入口：** Iteration 5 已关闭；严格从 Iteration 6 / 步骤 1 开始。

**目标：** 外部连接只由 Control Plane 认证和桥接；owner-sensitive live state 全部进入对应 owner worker。

- [x] **Control Plane bridge。** 负责 ticket/session/epoch admission、worker 路由、exact lease、single-use path-bound bootstrap、双向 OWP1 relay、有限 queue/明确 timeout、task cancellation 与 close propagation；不得解析 owner-sensitive stream 内容。（2026-07-11：`_OwnerWorkerWsBridge` 统一 relay task cancellation、双端 close 与 exact lease 单次 release；bounded queue/operation timeout 的 backpressure 为 1013；OWP1 保持 protocol-only framing/sequence validation。focused 12 passed，证据索引 I5/1。）
- [x] **Worker live state。** 将 PTY/browser map、live session、active slot 和 reconnect state 迁到 worker owner scope；attach/input/resize/checkpoint/resume/teardown 均绑定 owner、generation、epoch 和 internal audience。（2026-07-11：`OwnerWorkerLiveState` 是 entrypoint 创建的 worker app-local container；PTY owner registration 使用 immutable bootstrap fence，A/B 同 browser/channel 与 stale cleanup 均隔离；证据索引 I5/2。）
- [x] **Frontend ownership。** 以 `owner_key` 分桶 browser ID；logout 与 tenant/user switch 关闭旧 WS/PTY，清空 reconnect queue 和旧 owner context。（2026-07-11：`DashboardAuthTransition` 协调同 owner no-op、owner switch 与 logout teardown，ChatPage/ChatSidebar/GuiChatShell 均注册 scoped cleanup；证据索引 I5/3。）
- [x] **生命周期关闭。** worker crash、generation revoke、membership revoke 或 logout 时，Control Plane 与 worker 关闭/隔离长连接；旧连接不得静默迁到 global runtime 或新 owner。（2026-07-11：exact worker fence 的 revoke 用 1011 完整关闭 relay/worker half/release lease；authority scope/epoch revoke 保持 4401；replacement 与 different lease 隔离；证据索引 I5/4。）

**完成条件：** 同一浏览器在 A/B 间切换连接不同 worker，不能互踢、复用 PTY 或恢复 live session；cold start/crash/IPC timeout 有明确错误和关闭传播；旧 ticket/internal capability 不能用于新 generation。

</details>

<details>
<summary>迭代 6：Gateway、TUI、resume 和运行时索引（已完成，2026-07-11，4/4）</summary>

## 迭代 6：Gateway、TUI、resume 和运行时索引

> **执行状态：** `已完成`（2026-07-11） · **步骤：** `4/4` · **恢复入口：** 迭代 6 已关闭；严格从迭代 7 / 步骤 1 开始。

**目标：** 消除共享 map、recovery 和 routing index 导致的跨 owner 恢复。

- [x] Gateway/TUI 仅由 owner worker 运行，或由其 spawn 并通过 owner-bound IPC 接入。（2026-07-11：owner worker app 绑定 immutable exact runtime fence；Control Plane bridge 继续以 OWP1 接入，PTY child 用 app-local 60 秒 single-use attach URL；missing/expired/replayed/foreign token 或 runtime 均 fail closed。owner-required TUI 无 attach URL 时不再 spawn standalone `tui_gateway.entry`。I6/1 证据见 append-only 索引。）
- [x] 将 browser→PTY、session→live session、active slot、prompt cache、sessions index、channel directory、aliases 和 mirror lookup 放进 owner-local scope。（2026-07-11：`OwnerWorkerGatewayRuntime.mutable_state` 隔离 live session、pending/answer、child-mirror/active-run maps 与 locks；runtime-bound timers/threads 不退回 standalone state，`gateway.session`、channel directory/aliases、mirror index 保持 owner-home/owner-filter 解析；证据见 append-only 索引 I6/2。）
- [x] 必要持久 metadata 增加 owner/workspace/generation 断言；mismatch 按不存在处理并产生去敏安全事件。（2026-07-11：session index/origin、channel directory 与 authenticated alias/mirror read 均验证 trusted owner、canonical workspace、worker generation；缺失、无效或错配均按不存在处理，并由无自由字段的 Control Plane authority audit 仅记录 fixed event/reason/audience/opaque correlation ID/current generation；证据见 append-only 索引 I6/3。）
- [x] resume、latest descendant、channel/mirror/peer recovery 先 owner-scoped resolve，后读取 lineage、prompt 和 live state。（2026-07-11：SQLite row 持久化 owner/canonical workspace/generation；`session.resume` 先读取最小 scope record 并验证，再进行 title、descendant、live/liveness、reopen 或 history；compression tip、resume lineage、ancestor history 与 peer recovery 使用 exact durable predicate。foreign/missing/stale candidate 仅返回普通不存在；I6/4 证据见 append-only 索引。）

**完成条件：** stale session index、channel、mirror、TUI reconnect 和 Gateway recovery 均不能跨 owner；A 使用 B 的标识只得到不存在。已满足；完整 I6 单元回归 498 passed。

</details>

<details>
<summary>Iteration 7：Safe Filesystem Abstraction（已完成，2026-07-12，5/5）</summary>

## Iteration 7：Safe Filesystem Abstraction

> **执行状态：** `已完成`（2026-07-12） · **步骤：** `5/5` · **恢复入口：** Iteration 7 已关闭；严格从 Iteration 8 / 步骤 1 开始。

**目标：** 用 descriptor-relative、抗 TOCTOU 的文件协议替代字符串路径校验。

- [x] 定义 controlled root FD、只读 global root、owner writable root、workspace root 和 temporary root；不可信输入只允许相对路径。（I7/1 证据见 append-only 索引。）
- [x] Linux 优先 `openat2(RESOLVE_BENEATH|RESOLVE_NO_SYMLINKS|RESOLVE_NO_MAGICLINKS)`，按边界选择 `RESOLVE_NO_XDEV`；fallback 使用 root-FD-relative `openat`/`dir_fd`、`O_NOFOLLOW`、类型检查和 `fstat` 复核。（I7/2 证据见 append-only 索引。）
- [x] 逐类迁 files API、上传/下载、解包、rename/delete、checkpoint、logs、runtime socket、temp、workspace/cwd 和 subprocess cwd。（I7/3 证据见 append-only 索引。）
- [x] Compatibility Layer 仅暴露已安全选定的 `<runtime_owner_home>/workspaces/` cwd/项目视图；路径字符串不可再次成为授权输入。（I7/4 证据见 append-only 索引。）
- [x] 移除 import-time owner-sensitive path cache；默认路径仅在 runtime owner home 固定后加载或运行期解析。（2026-07-12：`CHECKPOINT_BASE` 仅作显式测试 override，default 由 `_effective_checkpoint_base()` 动态解析；移除 file-operation 死缓存；checkpoint CLI 使用 status 的 live base。I7/5 证据见 append-only 索引。）

**完成条件：** traversal、absolute/symlink/magic-link、race、bind mount、hard-link、archive path、TOCTOU 和 checkpoint poisoning 不能逃逸允许根。已满足；I7/5 相关单元回归 258 passed。

</details>

<details>
<summary>Iteration 8：隔离 Tool Executor、凭据和 FD（已完成，2026-07-12，5/5）</summary>

## Iteration 8：隔离 Tool Executor、凭据和 FD

> **执行状态：** `已完成`（2026-07-12） · **步骤：** `5/5` · **恢复入口：** Iteration 8 已关闭；严格从 Iteration 9 / 步骤 1 开始。

**目标：** Tool Executor 是 owner/workspace/generation/task-bound 的受限执行体。

- [x] 拆为独立 OS process/container runtime，绑定 owner/workspace/generation/task identity 与生命周期。（I8/1 证据见 append-only 索引。）
- [x] 仅传最小 env：runtime home、选择的 workspace、受限 tmp、任务参数和 egress profile；默认 `close_fds`、env allowlist、固定 cwd、isolated IPC。（I8/2 证据见 append-only 索引。）
- [x] 禁止继承 Control Plane socket、internal capability、external WS ticket、私钥、replay state、supervisor metadata、Docker socket、无关 FD、host PID/IPC 或完整环境。（I8/3 证据见 append-only 索引。）
- [x] credential broker 仅发放精确 task/audience/operation/scope/generation/expiry 的短期最小 capability；revoke/generation stop 立即废止并清理。（I8/4 证据见 append-only 索引。）
- [x] process registry、output、checkpoint、cancel/kill、terminal multiplex 和 child cleanup 均绑定 executor identity/generation。（2026-07-12：canonical immutable `ExecutorIdentity` fence 覆盖 direct process controls、checkpoint recovery、completion/watch queue 和 terminal multiplex；authenticated routing 对 owner/worker/generation/lease/recovery mismatch fail closed；revoke/generation stop reaps exact registry descendants；I8/5 证据见 append-only 索引。）

**完成条件：** 已满足；A/B process control、checkpoint、output/terminal routing 与 generation cleanup 单元回归 525 passed、1 warning。

</details>

<details>
<summary>Iteration 9：per-owner execution sandbox 策略代码（已完成，2026-07-13，5/5）</summary>

## Iteration 9：per-owner execution sandbox 策略代码

> **执行状态：** `已完成`（2026-07-13） · **步骤：** `5/5` · **恢复入口：** Iteration 9 已关闭；严格从 Iteration 10 / 步骤 1 开始。

**目标：** 仅编写 authenticated 多用户 sandbox 的纯策略/校验代码与单元测试；不启动真实 Docker/宿主部署。

- [x] 构建 owner/generation-bound sandbox launch spec；禁止跨 owner 复用 container、安全主体、tmpfs 或 mount view。（2026-07-12：`SandboxLaunchBinding` 将每个 dispatch 的 immutable executor identity、canonical owner runtime 和 fresh sandbox ID 绑定为独立 mount-view/tmpfs/security-subject identity；supervisor 在目录/FD/pipe/`Popen` 前 exact-match active lease，Bubblewrap builder 拒绝与 binding 不一致的 owner/runtime path；证据见索引 I9/1。）
- [x] 定义 immutable verification record 并校验 image digest、profile、owner/generation、UID/GID、mount、network/egress、rootfs 和安全控制；缺失/陈旧/失配拒绝。（2026-07-12：host-side `SandboxVerificationRecord` 与 frozen policy 在 launch side effects 前精确比对 binding/egress/security fields；record source 缺失、异常、空、陈旧或错配均拒绝，且 evidence 不跨入 child env/argv/bootstrap；证据见索引 I9/2。）
- [x] 构建/校验 allowlisted mounts：仅当前 `host_owner_home`、必要只读 global 资源与受限 tmpfs 进入 runtime；拒绝 `users/` 父级、其他 owner、可写 global、Docker socket、host `/proc`/`/sys`、host network/PID/IPC 和 privileged 配置。（2026-07-13：`SandboxMountPolicy` 固定 exact owner-generation runtime、readonly globals、descriptor workspace 与 size-bounded root/executor tmpfs；record 的 `mount_policy_id` 精确绑定 topology，危险 source/错配 evidence 在 launch preparation 前拒绝；证据见索引 I9/3。）
- [x] 构建 non-root、read-only rootfs、no-new-privileges、capability drop 与受限 syscall/capability 策略；记录或配置失效即拒绝 admission。（2026-07-13：frozen `SandboxSecurityPolicy` 将 profile/backend、UID/GID、rootfs/NNP、empty capability contract 和 syscall policy ID/digest 统一绑定；record/source/filter 任何缺失、失配或不可用均在 mkdir/FD/pipe/env/spec/`Popen` 前拒绝。Bubblewrap 必须支持并显式使用 `--uid`、`--gid`、`--cap-drop ALL`、`--seccomp`，policy-bound filter FD 仅经 `pass_fds` 传给 launcher，证据见索引 I9/4。）
- [x] 纯代码选择/拒绝 `control-only`、`owner-public`、`tool-none`、`tool-public`、`protected-target` egress profiles。（2026-07-13：封闭 `EgressProfile` vocabulary 与 immutable `ExecutorEgressPolicy` 仅以 trusted `function_name` 选择 canonical profile；Tool Executor 仅接收 `tool-none`、`tool-public`、`protected-target`，其余 recognized/unknown/malformed 值均在 launch preparation 前 fail closed。verification record、executor env 与 bootstrap 共享 strict parser，missing bootstrap 不回退；未改变 Bubblewrap network mode 或启用实际 egress，证据见索引 I9/5。）

**完成条件：** 已满足；跨 owner spec、危险 mount、特权配置、未批准 egress 与 shared-home fallback 均由纯代码/单元测试拒绝。

</details>

<details>
<summary>Iteration 10 — 资源治理、审计和单元回归（已完成，2026-07-13）</summary>

## 迭代 10：资源治理、审计和单元回归

**目标：** 编写资源治理与去敏审计决策代码，并用 A/B 单元矩阵证明 owner 隔离。

- [x] 定义 owner/workspace/generation 的 worker、CPU、memory、pids、disk/inode、FD、duration、concurrent tool、PTY、WS、Gateway channel、output volume 和 network quota 配置/判定。（2026-07-13：`ExecutorResourceQuota` 对 15 个资源维度施加 complete positive-integer schema，`ExecutorResourceDecision` 使用 exact immutable `ExecutorIdentity` 推导 de-identified policy ID；`ToolExecutorSupervisor` 在任何 resource source 缺失/异常/foreign identity 情形于 runtime/FD/spawn 前 fail closed，bootstrap 经 `ExecutorInvocation`/`invocation_from_payload()` 重建并验证完整 decision。focused 102 passed；相关回归 293 passed、1 existing warning；证据见索引 I10/1。）
- [x] 定义公平 admission、启动限流、idle timeout、每 owner 并发和仅 idle worker LRU eviction；不得为冷启动跨 owner 复用 runtime。（2026-07-13：`OwnerWorkerSupervisor._admit_start()` 集中 per-owner cooldown 和 capacity admission；`max_owner_concurrency` 在 active lease 验证后限制 exact handle uses。capacity eviction 只选 live idle LRU，不依赖 normal idle timeout；无 idle candidate 时 claim/spawn 前拒绝。A 被驱逐后以 existing exact drain/bridge-close/terminate 流程关闭，B 以独立 owner home/socket/generation cold start。focused 6 passed；相关回归 525 passed、10 existing warnings；证据见索引 I10/2。）
- [x] 构建 authority、ticket/capability consume/reject、epoch revoke、generation rollover、route/PTY lifecycle、filesystem deny、executor、credential、egress 和资源拒绝的去敏审计事件。（2026-07-13：`audit_authority()` 固定为 Control Plane-only closed event/reason/metadata contract；ticket mint/consume/reject、logout epoch revoke、worker generation transition、owner-worker bootstrap、filesystem deny、executor resource/egress/sandbox/lease reject 与 credential reject/revoke 均在高层可信边界发出 de-identified record。无 owner/home/path/route/ticket/token/JTI/capability/prompt/body/tool args/PTY bytes/exception escape hatch；audit delivery failure 不改变 fail-closed 或 revoke。focused 209 passed；相关回归 360 passed、1 existing `audioop` deprecation warning；证据见索引 I10/3。）
- [x] 建立 HTTP/WS/PTY/TUI/Gateway/session/files/checkpoint/executor 的 A/B 路由、配额、撤销与 owner-isolation 单元矩阵。（2026-07-13：verified HTTP session A/B 只能获得各自 derived exact owner handle；foreign/mismatched handle 在 acquire/request 前拒绝，authenticated Control Plane filesystem/checkpoint APIs 维持 403。scope/epoch 与 exact worker-generation bridge revoke 只关闭 A 且 exact lease 仅 release 一次；B bridge 保持 live。PTY active-session、Gateway attach/runtime、descriptor workspace/checkpoint 均 owner-local，A cleanup/拒绝不影响 B。executor credential/process revoke 和 implemented per-owner cooldown/concurrency/idle-only capacity 维持 A/B noninterference；未把 decision schema 误称 numeric quota enforcement，亦未声称 FIFO fairness。focused 581 passed、10 existing warnings；相关回归 739 passed、10 existing warnings；证据见索引 I10/4。）
- [x] 执行 migration/compatibility 单元回归：local legacy 独立，authenticated 无 global fallback，删除已被 owner worker 替代的 legacy bypass。（2026-07-13：local token mode 的 representative list/detail/delete session route 只打开 local profile-backed `SessionDB`，不会使用 supervisor 或 Owner Worker client。authenticated session、analytics、model-info worker request failure 统一 502，且 `SessionDB`、profile-home、config/analytics helpers 均由 sentinel 证明不可达；owner selector 在 proxy/fallback 前 400。explicit allowlist 仅放行已迁移 exact route，并拒绝 unknown session subpath、adjacent analytics/model、profiles/checkpoints/global-management bypass。focused 110 passed、1 existing warning；相关回归 540 passed、10 existing warnings；证据见索引 I10/5。）

**完成条件：** A 不侵占 B 的最低服务；revoke/stop 清理 registry/grant；correlation ID 只关联去敏授权与执行决策。已满足。

</details>

## 当前迭代：无

> 用户指定的 Iteration 10 已完成；未开始新的迭代。

## 记录与关闭

- 每完成一个 checkbox，仅一次性更新执行索引、状态总览、checkbox 和 append-only 证据索引；blocker 只更新执行索引并停止。
- 只有本迭代全部步骤、A/B 负例、规定单元测试和完成条件均满足后，才能关闭迭代并把内容折叠归档。
- 每个已关闭步骤必须保留修改文件、精确测试命令/结果、warnings、首个根因与最小复现（若有）。
- 已关闭 Iteration 1–4 的下列证据是审计记录，除非发现事实错误不得改写。

## 统一验证策略

每个迭代只新增或更新单元测试：覆盖 owner/capability/generation/lease/epoch/path/sandbox/resource 的正反例，以及 scope 外查找、错误身份、过期或重放凭据、危险路径/环境/sandbox spec 和 global/legacy fallback 的拒绝语义。最终标准是可信 OwnerContext 只路由到单 owner runtime，且 Control Plane、Worker 与 Tool Executor 的文件、进程、凭据、网络和资源边界均有代码和单元测试证据。
