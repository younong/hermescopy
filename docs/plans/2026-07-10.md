# 多用户隔离小步开发计划

## Context

本文把 `2026-07-07-multi-user-isolation-plan.md` 的目标架构拆为可独立实现、测试和合并的开发迭代，而不是发布/灰度计划。

目标是不让 authenticated 多用户模式只依赖“每条路径都传递 `owner_key`”的约定：可信身份派生与路由可以共享，但 owner 的数据、live state、PTY、prompt、memory、skills、workspace、进程环境和执行沙箱不能共享。任一 owner context、授权状态或运行时身份缺失/错配时必须 fail closed，不得回退至 global 或 legacy home。

## 共同开发规则

- 每个迭代是独立代码变更：先编写 A/B 隔离负例和失败语义的单元测试，再迁移对应入口。
- **分支纪律：** 所有计划内实现必须在当前 Git 分支开发；不得为执行本计划新建、切换或重置分支。开始实施前在“当前交接快照”记录当前分支；如当前分支不符合预期，先停止并记录 blocker，等待明确指示。
- **顺序执行纪律（强约束）：** 必须严格按本文迭代编号及每个迭代内复选框的文档顺序开发：下一项工作始终是最早一个未完成迭代的第一个未勾选步骤。所有前置步骤完成、其安全语义和单元测试证据已写回总览与“当前交接快照”之前，不得实施、以实施为目的测试或标记进行中任何后续步骤或后续迭代。唯一例外是先明确记录 blocker/决策并修订本计划以保留依赖关系；不得静默跳过、并行越级或把后续工作提前记为完成。
- **顶层上下文纪律（强约束）：** 本文档是本计划实施的顶层、权威上下文和唯一执行状态来源，优先于任何会话摘要、临时任务列表、口头交接或推测。开始工作、上下文压缩/恢复、交接或收到与本文冲突的后续请求时，实施者必须重新读取本文的共同开发规则、总览、当前交接快照和目标迭代，且不得用摘要替代原文、删减其约束或因上下文压缩而遗忘既有状态。若运行环境无法保留完整正文，必须停止计划内实施，先重新加载本文并恢复其作为顶层上下文；不得基于不完整上下文继续执行。
- **严格执行范围：** 本计划实施阶段只允许编写代码和运行单元测试（含相关单元回归）。不执行也不要求集成、多进程、多副本、E2E、smoke（冒烟）、应用运行、Docker/部署验证、发布、PR、提交、code review、security review、文档交付或任何其它验证活动；不得自动调用或要求 `/code-review`、`/review`、`/security-review`、`/verify` 或 `/run`。
- **子 Agent 上限：** 为执行本计划而创建的子 Agent 总数在整个任务生命周期内必须不超过 **10 个**（包含并行、后台、重试和补充调查的 Agent）。优先复用已有 Agent 或由主 Agent 顺序完成工作；达到上限后不得再创建 Agent，并在“当前交接快照”记录已用数量、未完成工作和后续接手方式。
- `owner_key`、`profile`、`browser_id`、session ID、cwd 和路径都不是授权输入；owner 只从可信 HTTP session、已验证 WS ticket 或 server-spawn context 派生。
- local/unauthenticated legacy 模式保持显式独立，绝不作为 authenticated 请求的 fallback。
- 未迁移的 owner-sensitive endpoint 明确返回 403/404；不得由 Control Plane 直接执行以保持“兼容”。
- 安全审计只能记录去敏 owner 摘要、scope、generation、事件和 correlation ID；不得记录 prompt、会话正文、原始 token/ticket 或敏感文件内容。

### 通用执行效率与质量控制（适用于后续所有步骤）

以下规则用于缩短实现—定位—回归循环；它们不得降低 fail-closed 要求、隔离边界、既有兼容契约或本计划限定的单元测试证据。

- **先收敛状态模型，再改入口：** 对涉及 credential、generation、lease、rotation、recovery、连接或生命周期的步骤，先在该步骤的测试/实现设计中明确状态、允许转换、终态、并发竞争点和每个异常输入的 fail-closed 结果；特别区分首次 bootstrap、正常运行、替换/丢失和显式 recovery，避免实现后才补状态语义。
- **先枚举不可破坏的外部契约：** 修改已有认证、ticket、worker、WS 或存储模块前，先从现有单元测试与调用点列出公开函数签名、错误分类/响应、单次消费、并发和兼容行为；安全升级应在边界上保持必要的泛化错误与既有调用契约，避免大范围重写后再逐项恢复。
- **按最小可验证切片推进：** 将一个步骤拆成可独立证明的纵向切片，例如“持久状态及纯函数 → 单模块 API → 调用入口 → 拒绝路径/审计”；每个切片先补对应正反例单元测试并通过，再连接下一层。不得提前实现后续复选框或以临时 fallback 连接未完成边界。
- **测试由窄到宽并保留失败信号：** 先运行直接模块/新用例的快速单测，修复后再运行该步骤列出的相关联合回归；不要在纯局部失败尚未定位时反复运行慢速全量相关套件。每次失败记录首个根因、受影响契约和修复后的最小回归命令，避免同类问题重复调查。
- **隔离序列化与持久化边界：** 对 keyring、SQLite metadata、token claim 或审计记录，明确区分内存对象和磁盘/线上的 schema，并为 parse、serialize、原子写入、权限检查、并发初始化和恢复分别测试；不得让 dataclass/运行时对象隐式穿透 JSON 等持久化格式。
- **限制变更面并及时回退无关改动：** 每个步骤只修改实现该步骤所需的模块与测试。若回归显示通用组件的既有契约不应变化，应立即将无关的行为扩张回退，并把更严格的语义限制在专用边界（例如 authority-specific audit），而非扩大到所有历史调用方。
- **把昂贵回归放在稳定点：** 在关键状态机、错误映射和兼容测试稳定后只运行一次完整相关单元回归；若失败，先以最小复现测试修复，再重新运行完整相关回归。完成时记录实际命令、总耗时、warning 数量及是否为既有 warning，供下一步骤选择合适的验证范围。
- **交接记录应保留决策，而非逐字过程：** 每完成或阻塞一个步骤，在“当前交接快照”记录状态模型的关键安全决定、已验证的兼容边界、仍存在的 blocker、修改文件和精确测试证据；这样上下文恢复时不必重新推导已解决的设计问题。


## 执行进度与交接记录（必须维护）

本节是本计划的**唯一执行状态来源**。实施 AI 在开始任何步骤前必须先读取本节和目标迭代；每完成、阻塞或撤销一个步骤后，立即更新对应复选框、状态、验证证据和“当前交接快照”。这样后续 AI 只需从第一个未完成的步骤继续，不能把已勾选项目重新实现。

- **状态值：** `未开始`、`进行中`、`阻塞`、`已完成`、`已废弃`。状态变化要附带日期和简短原因。
- **复选框语义：** 仅当代码、测试和该步骤要求的安全语义均完成时，才将 `[ ]` 改为 `[x]`；调查、部分实现或仅编写代码均不可勾选。
- **单元测试证据：** 每次状态更新写入实际运行的单元测试命令、结果和相关测试文件/用例。失败测试必须保留失败摘要和下一步，不得写成通过。
- **范围纪律：** 发现额外代码或单元测试工作时，先在相应迭代追加新的未勾选条目（或链接独立计划），再实施；不得默默扩展已完成步骤，也不得将非代码或非单元测试活动纳入本计划。
- **交接纪律：** 暂停、切换迭代或出现 blocker 时，更新下方快照。快照必须写明当前迭代/步骤；其“下一条可执行动作”必须是依照“顺序执行纪律”确定的最早未完成迭代的第一个未勾选步骤。已完成迭代的单元测试证据不得删除。

### 总览

| 迭代 | 状态 | 步骤进度 | 代码状态 | 单元测试证据 |
| --- | --- | --- | --- | --- |
| 1：OwnerContext 和 owner home | 已完成（2026-07-10） | 6/6 | canonical identity/keyring/统一派生/路径职责；host admission、authenticated profile boundary 与 owner-bound WS capability 已 fail closed | focused web server：25 passed（11.94s）；完整 web server：358 passed（73.64s）；auth/WS/web server 联合回归：454 passed（71.59s） |
| 2：共享授权状态、epoch 和 WS ticket 撤销 | 已完成（2026-07-10） | 6/6 | shared authority change dispatch、rotation/continuity fail-closed 与 Control-Plane 去敏审计已完成 | authority + ticket + audit + WS/middleware/web-server：528 passed（93.99s） |
| 3：Worker generation 和 owner-bound 控制握手 | 未开始 | 0/6 | — | — |
| 4：session API 迁入 Owner Worker | 未开始 | 0/5 | — | — |
| 5：WS、PTY 和浏览器连接边界 | 未开始 | 0/4 | — | — |
| 6：Gateway、TUI、resume 和运行时索引 | 未开始 | 0/4 | — | — |
| 7：Safe Filesystem Abstraction | 未开始 | 0/5 | — | — |
| 8：隔离 Tool Executor、凭据和 FD | 未开始 | 0/5 | — | — |
| 9：Docker per-owner execution sandbox | 未开始 | 0/5 | — | — |
| 10：资源治理、审计和全链路回归 | 未开始 | 0/5 | — | — |

### 当前交接快照

> **最后更新：** 2026-07-10（迭代 2 / 步骤 1–6 已完成）。  
> **当前迭代 / 步骤：** 迭代 2 已完成；下一项为迭代 3 / 步骤 1。  
> **下一条可执行动作：** 严格执行迭代 3 / 步骤 1：为 Owner Worker 引入 generation 和 owner-bound 控制握手。  
> **已完成：** 迭代 1 / 步骤 1：现有 frozen `OwnerContext`、`owner_context_from_session()` 和 `owner_context_from_ticket_payload()` 作为唯一可信派生入口；新增 A/B principal 隔离、不可变性、缺失可信 user ID、HTTP/WS/worker context 一致性及 A ticket 不可重建为 B 的测试。迭代 1 / 步骤 2：`owner_context.py` 现有 `ok1_` 兼容性上新增私有 JSON keyring、active/retained/pending metadata、备份路径声明及显式 begin → migrate → complete rotation；冲突、未完成迁移、空/并发 pending secret 和环境 active-secret 不一致均 fail closed。迭代 1 / 步骤 3：`/api/auth/me`、`/api/auth/ws-ticket` 使用同一 verified session 派生，WS payload 重新验证 canonical owner，supervisor 将完整 canonical owner 传给 worker spawn；A/B 及 mismatch 单元测试已覆盖。迭代 1 / 步骤 4：`OwnerContext` 公开 `host_global_home` 与 `host_owner_home`（保留 `owner_home` compatibility alias）；`get_runtime_owner_home()` 明确 worker 运行时路径只从 worker 环境派生，路径字符串与 host view 相等不作为身份断言。迭代 1 / 步骤 5：`_admit_host_owner_home()` 仅接受 canonical host global/users/owner path；逐段 lstat 并拒绝 symlink、非目录、group/world writable home、异常 owner、非同 device mount 以及 admission 内 inode/type 替换，再创建 runtime layout。迭代 1 / 步骤 6：Control Plane 与 worker 均拒绝外部 owner/profile 选择。AF_UNIX blocker：健康测试改用短的、per-process `/tmp/h<pid>` socket root，并向 worker 显式传入匹配的 `HERMES_HOME`/`HERMES_OWNER_KEY`，避免 macOS 104-byte pathname 限制。  
> **进行中的改动：** 迭代 2 / 步骤 1–6 已完成：browser ticket keyring 使用 Control-Plane-only root、active/retained/pending rotation 和 bounded retained verification；authority/keyring replay witness 在 store loss、metadata failure 或 recovery uncertainty 时阻断 mint/verify/consume，显式 recovery 递增 generation 并失效旧 ticket。authority audit 仅写 Control Plane `logs/authority.log`，要求 server-generated correlation ID 且只含 allowlisted digest/epoch/reason/audience-class 字段；HTTP mint 和 WS admission 对 authority outage 503/`authority_unavailable` fail closed，并不会记录 raw ticket 或 exception。  
> **阻塞项 / 决策：** 无未解决 blocker。仍不得将此 WS upgrade revalidation 当作 refresh：缺失/过期 access-session cookie 须先经 HTTP gate 重建 verified session 后重新铸造 ticket。`git diff --check` 对本步骤源码与测试无输出；本文件 Markdown 硬换行仍使用 two-space trailing whitespace。  
> **修改文件：** `hermes_cli/dashboard_auth/authority.py`、`hermes_cli/dashboard_auth/ws_tickets.py`、`hermes_cli/dashboard_auth/audit.py`、`hermes_cli/dashboard_auth/routes.py`、`hermes_cli/web_server.py`、`tests/hermes_cli/test_dashboard_auth_authority.py`、`tests/hermes_cli/test_dashboard_auth_ws_tickets.py`、`tests/hermes_cli/test_dashboard_auth_audit.py`、`tests/hermes_cli/test_dashboard_auth_ws_auth.py`、`tests/hermes_cli/test_dashboard_auth_middleware.py`、`tests/hermes_cli/test_web_server.py`、`docs/plans/2026-07-10.md`。  
> **验证命令与结果：** 迭代 1 的既有验证记录保留；迭代 2 / 步骤 6 focused：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_authority.py`：通过，62 passed，2.18s；完整相关单元回归：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py`：通过，528 passed、10 warnings，93.99s。  
> **当前分支：** `feature/multi-user-owner-workers`。  
> **代码状态：** 工作区保留既有未提交改动；迭代 2 已完成，未触及后续迭代。  

## 迭代 1：统一 OwnerContext 和 owner home

> **执行状态：** `已完成`（更新日期：2026-07-10；步骤 1–6、AF_UNIX、WS auth 和完整 web-server regression 均已收敛） · **步骤：** `6/6` · **代码状态：** canonical identity、keyring/rotation、统一派生、host/runtime path contract、Control Plane host-home admission、authenticated profile boundary 与 owner-bound WS capability 已闭环 · **单元测试：** focused web server：25 passed（11.94s）；完整 web server：358 passed（73.64s）；auth/WS/web server 联合回归：454 passed（71.59s）  
> **恢复入口：** 迭代 1 已关闭。迭代 2 仍未开始；仅在收到后续明确执行指令后，严格从迭代 2 的第一个未勾选条目开始。

**目标：** 固定身份、命名和目录选择，避免 HTTP、WS、worker spawn 对同一用户得出不同 owner。

- [x] 提供唯一的不可变 `OwnerContext` 派生模块，固定 `auth_provider + tenant_id + owner_user_id` 的 canonical 规则。（2026-07-10：复用 `hermes_cli/dashboard_auth/owner_context.py` 的 frozen `OwnerContext`、`owner_context_from_session()`、`owner_context_from_ticket_payload()`；在 `tests/hermes_cli/test_dashboard_auth_ws_tickets.py::TestOwnerContext` 增加 A/B principal、不可变性、缺失可信 user ID、HTTP/WS/worker 一致性和 ticket owner mismatch 负例。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py -k TestOwnerContext`，12 passed、19 deselected，2.33s。）
- [x] 使用带版本前缀的 HMAC 生成不可预测、文件系统安全的 `owner_key`；定义 secret 持久化、备份和显式轮换迁移。（2026-07-10：保留 `ok1_` 兼容 HMAC，新增私有 `control-plane/owner_keyring.json` 的 active/retained/pending metadata、`owner_keyring_backup_paths()` 和显式 begin → migrate → complete 流程；环境 active-secret 不一致、空/冲突 pending secret、迁移目标冲突与未完成迁移均 fail closed。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py -k 'rotation or keyring'`，4 passed、31 deselected，0.46s；完整 `TestOwnerContext` 16 passed、19 deselected，0.60s。）
- [x] 让 HTTP session、`/api/auth/me`、WS ticket、worker spawn 和 server-spawn context 使用同一派生函数。（2026-07-10：`routes.py` 的 auth-me/ticket 和 `web_server.py` 的 authenticated proxy 复用 `owner_context_from_session()`；WS 使用 `owner_context_from_ticket_payload()` 重新验证 ticket principal；`OwnerWorkerSupervisor` 将 canonical owner 的 key/home/provider/tenant/user 传给 argv/env。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_owner_worker.py -k 'canonical_owner or distinct_users_mint or canonical_session_owner_context'`，3 passed、81 deselected，1.56s。）
- [x] 固定术语与职责：`host_global_home`、`host_owner_home`、`runtime_owner_home`。容器路径、容器 ID、环境变量或前端参数不能证明身份。（2026-07-10：`OwnerContext` 固定 host-side 路径并保留 `owner_home` 兼容 alias；`owner_runtime.get_runtime_owner_home()` 只读取 worker `HERMES_HOME`，`assert_owner_runtime_paths()` 以 runtime root 做自检。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_owner_runtime.py -k 'same_verified_session or runtime_owner_home or assert_owner_runtime_paths'`，3 passed、41 deselected，0.48s；`tests/hermes_cli/test_owner_worker.py -k 'canonical_session_owner_context or supervisor_rejects_same_key'`，2 passed、20 deselected，0.53s。）
- [x] 由 Control Plane 从 `OwnerContext` 安全创建/确认 `host_owner_home`；拒绝 symlink、异常父目录、错误权限、非预期 inode/mount。（2026-07-10：`_admit_host_owner_home()` 从 canonical host global/users/owner key 构造路径，lstat 拒绝 symlink/非目录，拒绝 group/world writable/错误 ownership home，检查 users/owner device 一致并在创建后复核 inode/type；新增 users symlink 与 unsafe existing mode 负例。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_tickets.py -k TestOwnerContext`，18 passed、19 deselected，0.61s。）
- [x] authenticated 模式拒绝外部 `profile`，或仅映射到 owner-local 子命名空间，绝不能选择任意 DB/home。（2026-07-10：Control Plane proxy 在启动 worker 前拒绝 query `owner`、`owner_home`、`owner_key` 及非兼容 `profile`，只移除兼容 `default/current`；worker HTTP middleware 同样拒绝外部 owner selector，worker route 继续拒绝 legacy profile。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_web_server.py tests/hermes_cli/test_owner_worker.py -k 'authenticated_owner_key_query or authenticated_profile_selection or authenticated_templated_session_routes or worker_routes_reject_external_owner_selector or worker_session_routes_reject_legacy_profile_selection or worker_analytics_routes_reject_legacy_profile_selection'`，8 passed、373 deselected，2.06s。）
- [x] 处理迭代 1 相关 unit regression blocker：`tests/hermes_cli/test_owner_worker.py::test_worker_health_over_unix_socket_reports_owner_env` 在当前 pytest temp 根下生成超过 AF_UNIX 长度限制的 socket path。（2026-07-10：用户已明确许可修复/验证此多进程 unit regression；测试改为短的 per-process `/tmp/h<pid>` socket root，并向 child 显式传入匹配的 `HERMES_HOME`、`HERMES_OWNER_KEY`。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_owner_worker.py`，23 passed，9.01s。）
- [x] 收敛迭代 1 关闭前 WS auth unit regression：`tests/hermes_cli/test_dashboard_auth_ws_auth.py` 仍有 5 项旧断言要求 authenticated gated mode 使用 process-global `?internal=` credential 或构建携带它的 sidecar/gateway URL；这与当前 owner-bound internal capability 设计不一致。先新增/调整 A/B 失败语义覆盖，再按同一 capability 契约实现或移除旧行为，并运行完整相关 unit regression。（2026-07-10：将 gated mode 的 process-global credential 成功/复用断言改为 `internal_owner_context_required` 拒绝；将 Control Plane gated sidecar/gateway URL 断言改为 `None`；新增 worker-mode URL 仅含 `internal_owner_token`、绑定 owner/audience/exact path 且 A/B payload 不同的覆盖。生产边界已符合契约，无需修改 `web_server.py`。验证：`~/.local/bin/uv run --extra dev pytest tests/hermes_cli/test_dashboard_auth_ws_auth.py`，62 passed，2.63s。）
- [x] 收敛迭代 1 关闭前完整 web-server unit regression：将 plugin manifest 无认证旧预期改为 `401`（保持最小 public allowlist）；将 local `/api/model/info` 测试显式置于 loopback token contract，authenticated 请求仍代理 Owner Worker；将 PTY resolver doubles 更新为接受并验证 `browser_id`、`app_obj` 的 app/browser-aware 契约，未恢复任何 authenticated legacy/global fallback。（2026-07-10：focused `test_web_server.py -k 'test_unauthenticated_api_blocked or TestModelInfoEndpoint or TestPtyWebSocket'`：25 passed、333 deselected，11.94s；完整 `test_web_server.py`：358 passed，73.64s；middleware/WS auth/web-server 联合回归：454 passed，71.59s。）

**完成条件：** 同一登录态在 HTTP、WS 和 worker spawn 得到同一 owner key；owner 缺失/不一致即失败；重启后 owner key 稳定；A 不能通过 owner/profile/path 参数选择 B 的目录。

## 迭代 2：共享授权状态、epoch 和 WS ticket 撤销

> **执行状态：** `已完成`（更新日期：2026-07-10） · **步骤：** `6/6` · **代码状态：** shared authority/replay store、signed ticket、bridge revocation、key rotation/recovery continuity 与 Control-Plane de-identified audit 已闭环 · **单元测试：** authority/ticket/audit/WS/middleware/web-server：528 passed、10 warnings（93.99s）  
> **恢复入口：** 迭代 2 已关闭；严格从迭代 3 的第一个未勾选条目开始。

**目标：** 把 browser WS ticket 从进程内状态升级为跨 Control Plane 副本可原子消费和撤销的授权能力。

- [x] 建立 control-only shared authority/replay store，持久保存 authorization epoch、session/membership revoke、ticket consume/replay 和内部 capability revoke 元数据。（2026-07-10：新增 `hermes_cli/dashboard_auth/authority.py`，以 `control-plane/authority.sqlite3` 持久化 scope epoch、session revoke、membership revision、exact-audience credential replay key 和 recovery generation；SQLite `BEGIN IMMEDIATE`、symlink/权限/schema/transaction fail-closed 已覆盖。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py`，8 passed，0.52s。）
- [x] 实现原子操作：读取当前 epoch、revoke 并递增 epoch、exact-audience `jti` check-and-consume-until-expiry，以及恢复不可信时 outstanding credential 全部失效。（2026-07-10：`AuthorityStore.activate()`/`read_state()` 提供 epoch 读取；`revoke_and_bump()` 使 verified session scope 失效并递增 epoch；`check_and_consume()` 在 `BEGIN IMMEDIATE` transaction 内验证 epoch/recovery generation、清理过期记录并插入 `(token_class, issuer_key_version, jti digest, audience)` 唯一 replay key；`invalidate_outstanding_credentials()` 递增 recovery generation。A/B、竞争消费、expiry、exact audience 和 recovery mismatch 的单元测试已覆盖。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py`，8 passed，0.63s。）
- [x] browser WS ticket 只包含可重建 `OwnerContext` 的可信 principal 材料、issuer/key version、exact public route/protocol audience、短 TTL、epoch 和唯一 `jti`。（2026-07-10：`ws_tickets.py` 改为签名 `bwt1` capability，claims 包含 provider/org/tenant/user/owner_key、issuer key version、`jti`、`iat`/`exp`（30 秒）、authority epoch/recovery generation 与受限 `browser-ws:/api/{pty,ws,pub,events}` audience；不再使用进程内 browser ticket 字典。`verify_ticket()` 在未消费前验证签名、version、issuer、TTL、claims 和 exact audience，`consume_ticket()` 委托 authority store 的原子消费。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_authority.py`，47 passed，1.84s。）
- [x] Control Plane 在 WS upgrade 前验证 session/成员资格/epoch/audience，并在 shared replay store 中原子单次消费；ticket 永不转发 Worker。（2026-07-10：`middleware.verify_websocket_ticket_session()` 在 WS upgrade 中用 ticket 绑定 provider 重新验证 HttpOnly access-session cookie，交叉核对 provider/user/org、canonical OwnerContext 与 provider `authorization_state()` 的 session handle/membership revision；`_ws_auth_result()` 先验证签名和 exact `browser-ws:<ws.url.path>` audience、重建 owner，再执行该 current-session/membership 校验，最后才调用 authority store 原子 consume，因此失败 upgrade 不会烧毁 ticket。provider outage 返回 fail-closed `authority_unavailable`，其余认证失败发生在 accept/supervisor/bridge 前；既有 `_ws_query_for_owner_worker()` 持续剥离 browser ticket。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py`，505 passed、10 warnings，81.82s。)
- [x] logout、session expiry/revoke、tenant 切换和成员资格/角色变更撤销受影响 ticket 并关闭旧连接；无可信 session 的 logout 幂等且不影响其他 owner。（2026-07-10：`AuthorityStore` 追加跨 Control Plane replica 可读取的仅含 scope digest/epoch/revoked 的顺序变更记录；`web_server.py` dispatcher 按变更关闭本地匹配 bridge，authority 读取失败时 fail-close 关闭本地已认证 bridge。logout 只在 fresh verified session 后 revoke，provider outage/no session 不创建 wildcard revoke；HTTP/refresh authority activation 对 tenant/session/membership transition 失效旧 ticket 并关闭旧 epoch bridge；browser access-session expiry 关闭浏览器 half，bridge `finally` 关闭 worker half 并释放 lease。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_dashboard_auth_ws_auth.py`，119 passed、1 warning，5.77s；完整相关单元回归 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_web_server.py`，516 passed、10 warnings，87.40s。）
- [x] 定义 key rotation、authority outage、replay-store 恢复失败的 fail-closed 和去敏审计行为。（2026-07-10：browser ticket 使用独立 Control-Plane keyring 的 active/retained/pending state machine，rotation 仅接受 active signing、在 30-second TTL bounded window 内以 retained issuer 验证旧 ticket，未知/过期 issuer、pending/metadata/path failure 均拒绝；worker 环境必须显式提供 `HERMES_CONTROL_HOME`，不能写入 owner-local authority state。authority SQLite/keyring witness 同时证明 replay continuity，store/keyring replacement、不可用或 recovery-required 阻断 mint/verify/consume；显式 recovery 推进 generation，令旧未消费 ticket 失效，只允许新 ticket。authority audit 写入 Control Plane `logs/authority.log`，每条记录有 server-generated correlation ID，并且仅允许去敏 reason/audience-class/epoch/generation/digest 字段；HTTP mint 与 WS admission 的 outage 均 fail closed，WS 不记录 raw exception/ticket。验证：`~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_authority.py`，62 passed，2.18s；完整相关单元回归 `~/.local/bin/uv run --extra dev pytest -q tests/hermes_cli/test_dashboard_auth_audit.py tests/hermes_cli/test_dashboard_auth_authority.py tests/hermes_cli/test_dashboard_auth_ws_tickets.py tests/hermes_cli/test_dashboard_auth_middleware.py tests/hermes_cli/test_dashboard_auth_ws_auth.py tests/hermes_cli/test_web_server.py`，528 passed、10 warnings，93.99s。）

**完成条件：** A logout 后任一副本都拒绝 A 已签发未消费 ticket，B 不受影响；竞争消费只能成功一次；过期、错误签名、错误 route/audience、重复 jti、store 不可用均拒绝；重新登录的新 ticket 成功。

## 迭代 3：Worker generation 和 owner-bound 控制握手

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/6` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** 将 Owner Runtime Worker 变为可验证、可撤销、不可跨 owner 复用的 peer。

- [ ] 每次 worker 启动分配单调 `worker_generation`，建立 `starting → active → draining/revoked → terminated/failed` 生命周期。
- [ ] 用 authority-backed lease/CAS 处理同 owner 并发启动、crash、replace 和 idle recycle，避免 stale worker resurrection。
- [ ] 内部 capability 精确绑定 issuer/key version、owner identity、worker ID/generation、audience、scope、protocol version、短 expiry 和 `jti`。
- [ ] bootstrap capability 在 control-only replay store 单次消费；握手后连接绑定 peer identity、随机 nonce 和单调序号，拒绝跨连接重放与序号回退。
- [ ] Worker 在 owner-sensitive import 前固定 `HERMES_HOME=runtime_owner_home`，只接收最小 owner 环境，并自检 DB、runtime、logs、checkpoint、session index 与 workspace 都位于 runtime root。
- [ ] replace/revoke 时先废止 generation/capability、关闭旧连接，再清理进程/容器及临时资源。

**完成条件：** A/B 不共用 active generation；重启/PID reuse 后旧 health、token、route、PTY attach 和 IPC 均失败；owner/home/workspace/generation 任一错配不能接入。

## 迭代 4：将 session API 迁入 Owner Worker

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/5` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** Control Plane 只认证、路由和透传，绝不直接读取 authenticated owner 的数据或恢复状态。

- [ ] 先迁移只读 API：list、search、detail、messages、stats、export、latest-descendant/lineage。
- [ ] 再迁移写 API：rename/patch、delete、prune 和 resume 相关写入。
- [ ] Control Plane 从可信 session 重新派生 OwnerContext，只代理到健康且 generation 匹配的 worker，并携带内部 capability。
- [ ] 禁止 authenticated Control Plane 直连 `SessionDB`、global DB、profile 聚合和默认 home fallback；未迁移 surface 返回 403/404。
- [ ] session/title/short ID/resume ID 的查找必须在 worker owner scope 内完成；scope 外统一返回 404 或空结果。

**完成条件：** A/B 使用独立 owner-local `state.db`；authenticated 请求不读取 global DB；A 无法 list/search/detail/export/update/delete/prune/resume B 的 session；legacy 路径保持显式独立。

## 迭代 5：WS、PTY 和浏览器连接边界

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/4` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** 外部连接只由 Control Plane 认证和桥接；owner-sensitive live state 全部进入对应 worker。

- [ ] Control Plane 负责 ticket 认证、worker 路由、双向 bridge、backpressure、timeout 和 close propagation，不解析 owner-sensitive stream 内容。
- [ ] 将 PTY/browser map、live session、active slot、reconnect state 迁到 worker owner scope；attach/input/resize/checkpoint/resume/teardown 绑定 owner、generation、epoch 和内部 audience。
- [ ] 前端按 `owner_key` 分桶 browser ID；logout 和 tenant/user switch 时关闭旧 WS/PTY，清理 reconnect queue 和旧 owner context。
- [ ] worker crash、generation revoke、membership revoke 或 logout 时，Control Plane 与 worker 关闭/隔离长连接；旧连接不得静默迁到 global runtime 或新 owner。

**完成条件：** 同一浏览器 A/B 切换连接不同 worker，不能互踢、复用 PTY 或恢复 live session；cold start/crash/IPC timeout 有明确错误和关闭传播；旧 ticket/internal capability 不能用于新 generation。

## 迭代 6：Gateway、TUI、resume 和运行时索引

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/4` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** 消除共享 map、recovery 和 routing index 导致的跨 owner 恢复。

- [ ] Gateway/TUI 由 owner worker 运行，或只由 owner worker spawn 并经 owner-bound IPC 接入。
- [ ] 将 `browser_id → PTY`、`session_key → live session`、active slot、prompt cache、sessions index、channel directory、aliases、mirror lookup 放入 owner-local scope。
- [ ] 为必要持久 metadata 增加 owner/workspace/generation 断言；mismatch 按不存在处理并产生去敏安全事件。
- [ ] resume、latest descendant、channel/mirror/peer recovery 必须先 owner-scoped resolve，后读取 lineage、prompt 和 live state。

**完成条件：** 相同 platform/chat/thread 在 A/B 下不能复用 session；stale session index、channel、mirror、TUI reconnect 和 Gateway recovery 都不能跨 owner；A 使用 B 的 resume/session 标识仅得到不存在。

## 迭代 7：Safe Filesystem Abstraction

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/5` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** 以 descriptor-relative、抗 TOCTOU 的文件协议替代“`resolve()` 校验后按字符串路径操作”。

- [ ] 定义受控 root FD、只读 global root、owner writable root、workspace root、temporary root；不可信输入只允许相对路径。
- [ ] Linux 优先使用 `openat2` 与 `RESOLVE_BENEATH | RESOLVE_NO_SYMLINKS | RESOLVE_NO_MAGICLINKS`，按根边界需要增加 `RESOLVE_NO_XDEV`；fallback 使用 root FD 下逐段 `openat`/`dir_fd`、`O_NOFOLLOW`、类型检查和 `fstat` 复核。
- [ ] 将 files API、上传/下载、解包、rename/delete、checkpoint、logs、runtime socket、temp file、workspace/cwd 和子进程 cwd 逐类迁入该层。
- [ ] Workspace Compatibility Layer 仅向已安全选定的 `<runtime_owner_home>/workspaces/` 提供普通 cwd、`CLAUDE.md` 和项目工具视图；已验证路径字符串不能成为后续授权输入。
- [ ] 审计 import-time path cache，使 owner-sensitive 默认路径只在 runtime owner home 固定后加载或在运行期解析。

**完成条件：** traversal、absolute path、symlink/magic link、rename race、bind mount、hard-link、解包路径、TOCTOU 和 checkpoint poisoning 均不能逃出允许根；generation 失效后旧 handle/checkpoint 不能继续使用；受支持 workspace 工作流可用。

## 迭代 8：隔离 Tool Executor、凭据和 FD

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/5` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** Tool Executor 成为受限执行体，而不是继承 worker/Control Plane 全部能力的线程或子进程。

- [ ] 将 Tool Executor 拆为独立 OS process/container runtime，按 owner/workspace/generation/task 绑定身份和生命周期。
- [ ] 仅传递最小化环境：`HERMES_HOME`、选定 workspace、受限 tmp、任务参数、egress profile；默认 `close_fds`、env allowlist、固定 cwd 和 isolated IPC。
- [ ] 禁止继承 Control Plane socket、内部 capability、external WS ticket、mTLS private key、replay state、supervisor metadata、Docker socket、无关 FD、host PID/IPC 和完整 Control Plane env。
- [ ] 引入 credential broker，只对精确 task/audience/operation/scope/generation/expiry 发放短期最小 capability；revoke/generation stop 时立即废止并清理。
- [ ] 让 process registry、output stream、checkpoint、cancel/kill、terminal multiplex 和 child cleanup 都绑定 executor identity/generation。

**完成条件：** A 的 executor 不能查看 B 的 process/mount/workspace/env/FD/credential/output；revoke 结束全部 descendants、撤销 grants 并清理 socket/registration；终端、checkpoint、cancel、恢复流程仍可运行。

## 迭代 9：per-owner execution sandbox 策略代码

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/5` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** 编写 authenticated 多用户 execution sandbox 的配置、策略构建和 fail-closed admission 代码，并用单元测试覆盖其隔离约束；不在本计划中启动或验证真实 Docker/宿主部署。

- [ ] 编写 per-owner sandbox 启动规格构建代码，绑定 owner、worker generation 和隔离身份；禁止跨 owner 复用 container、安全主体、tmpfs 或 mount-view 配置。
- [ ] 编写不可变 sandbox verification record 的数据模型和纯代码校验，绑定 image digest、profile、owner key、worker generation、UID/GID、mount、network/egress、rootfs 与安全控制；缺失、陈旧或失配时 fail closed。
- [ ] 编写允许的 mount-spec 构建与校验代码：仅当前 `host_owner_home`、必要只读全局资源和受限 tmpfs 可进入 `runtime_owner_home`；拒绝父级 `users/`、其他 owner home、可写 global root、Docker socket、host `/proc`/`/sys`、host network/PID/IPC 和 privileged 配置。
- [ ] 编写 non-root、read-only rootfs、no-new-privileges、capability drop 与受限 syscall/capability 的策略构建代码；安全记录或策略配置失效时拒绝 admission。
- [ ] 编写 `control-only`、`owner-public`、`tool-none`、`tool-public`、`protected-target` egress profile 的纯策略选择与拒绝逻辑；不执行真实 gateway/proxy、DNS 或连接验证。

**完成条件：** 单元测试证明 A/B 不能生成指向对方 DB、runtime、workspace、log、checkpoint 或 socket 的 sandbox 规格；authenticated 多用户配置不允许裸进程/shared-home fallback；失配 record、危险 mount、特权配置与未批准 egress profile 均被代码拒绝。

## 迭代 10：资源治理、审计和单元回归

> **执行状态：** `未开始`（更新日期：待开始） · **步骤：** `0/5` · **代码状态：** `—` · **单元测试：** `未执行`  
> **恢复入口：** 从本迭代第一个未勾选条目开始；完成后同步更新总览与“当前交接快照”。

**目标：** 编写资源治理与去敏审计代码，并用单元测试覆盖资源竞争和 owner 隔离决策；不做长时间运行、运行时恢复或全链路验证。

- [ ] 按 owner/workspace/generation 编写 worker、CPU、memory、pids、disk/inode、FD、duration、concurrent tool、PTY、WS、Gateway channel、output volume 和 network 配额的配置与判定代码。
- [ ] 编写公平 admission、启动频率限制、idle timeout、每 owner 最大并发和仅针对 idle worker 的 LRU eviction 决策代码；不得为了冷启动跨 owner 复用 runtime。
- [ ] 编写 authority decision、ticket/capability consume/reject、epoch revoke、generation rollover、route/PTY lifecycle、filesystem deny、executor lifecycle、credential grant、egress decision 和资源拒绝的去敏审计事件构建代码。
- [ ] 建立 A/B 单元测试矩阵，覆盖 HTTP/WS/PTY/TUI/Gateway/session/files/checkpoint/executor 的路由、配额、撤销和 owner 隔离决策；不执行 E2E 或运行时故障注入。
- [ ] 做 migration/compatibility 单元回归：legacy/local 保持独立；authenticated 不存在 global fallback；逐步删除已被 owner worker 替代的 legacy bypass。

**完成条件：** 单元测试证明 A 的资源治理决策不会侵占 B 的最低服务，revoke/stop 后相关 registry 和 credential 状态被清理，correlation ID 仅关联去敏授权与执行决策，且所有已实现的 A/B 负例和故障语义保持 owner 隔离。


## 每个迭代的关闭与交接清单

在将任一迭代标记为 `已完成` 前，逐项勾选，并将结果写回总览和交接快照：

- [ ] 本迭代全部步骤已勾选；任何取消/替代的步骤均有原因与替代项链接。
- [ ] 已新增或更新覆盖 A/B 隔离负例与失败语义的单元测试。
- [ ] 已运行本迭代单元测试和此前相关单元回归；命令、结果和未运行原因均已记录。
- [ ] 已核对完成条件的每个安全断言，没有 authenticated 到 legacy/global fallback。
- [ ] 已记录变更文件、当前分支、遗留风险、blocker 与下一迭代的第一个未完成动作。

## 统一验证策略

每个迭代只能新增或更新单元测试，并保持此前相关单元回归通过：

- [ ] **单元/属性测试：** owner 派生稳定性、ticket/capability claims、epoch/replay 语义、generation mismatch、路径协议、环境最小化、sandbox 策略和资源治理决策。
- [ ] **A/B 失败语义单元测试：** owner 范围外的查找、错误身份、过期或重放 credential、危险路径/环境/sandbox 规格以及 global/legacy fallback 必须被拒绝或按不存在处理。

最终的完成标准不是“所有路由都携带 `owner_key`”，而是：authenticated 请求只能由可信 `OwnerContext` 路由到单 owner runtime；Control Plane 不读 owner-sensitive state；Worker 和 Tool Executor 在文件、进程、凭据、网络和资源上的边界均由代码实现和单元测试覆盖。本计划不包含任何运行、集成、E2E、部署或生产形态验证。